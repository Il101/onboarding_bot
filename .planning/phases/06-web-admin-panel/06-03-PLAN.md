---
phase: 06-web-admin-panel
plan: 03
type: execute
wave: 2
depends_on: [06-01]
files_modified:
  - src/api/routes/admin.py
  - src/api/templates/knowledge/review.html
  - src/api/templates/knowledge/_edit_modal.html
  - src/api/templates/knowledge/_row.html
  - tests/test_admin.py
autonomous: true
requirements: [ADM-03, ADM-04, ADM-05]

must_haves:
  truths:
    - "Admin sees a table of knowledge items with fact, topic, confidence score, source attribution, and status columns"
    - "Admin can filter knowledge items by topic, source, and status (published/pending/rejected) per D-04"
    - "Admin can approve a single pending knowledge item (status changes to published) per ADM-04"
    - "Admin can reject a single pending knowledge item (status changes to rejected) per ADM-05"
    - "Admin can bulk-approve multiple selected knowledge items via checkboxes per D-04"
    - "Admin can bulk-reject multiple selected knowledge items via checkboxes"
    - "Admin can delete a knowledge item permanently per ADM-05"
    - "Admin can edit the fact text of a pending knowledge item before approval per D-04"
    - "Table supports server-side pagination (20 items per page default)"
  artifacts:
    - path: "src/api/routes/admin.py"
      provides: "Knowledge CRUD endpoints: list, approve, reject, delete, edit"
      contains: "knowledge"
      exports: ["GET /api/admin/knowledge", "POST /api/admin/knowledge/approve", "POST /api/admin/knowledge/reject", "DELETE /api/admin/knowledge/{item_id}", "PUT /api/admin/knowledge/{item_id}"]
    - path: "src/api/templates/knowledge/review.html"
      provides: "Knowledge review page with filters, table, and bulk actions"
      contains: "hx-get"
    - path: "src/api/templates/knowledge/_row.html"
      provides: "Single knowledge row HTMX partial for table updates"
      contains: "KnowledgeStatus"
  key_links:
    - from: "src/api/templates/knowledge/review.html"
      to: "GET /api/admin/knowledge"
      via: "htmx fragment load with filter params"
      pattern: "hx-get.*admin/knowledge"
    - from: "src/api/routes/admin.py"
      to: "src/models/knowledge_item.py"
      via: "SQLAlchemy query for KnowledgeItem"
      pattern: "db.query\\(KnowledgeItem\\)"
---

<objective>
Implement the Knowledge review section: a filterable table of extracted knowledge items with approve, reject, delete, edit, and bulk operations. This is the core review gate (D-03, D-04) that lets administrators control what enters the RAG pipeline.

Purpose: Delivers ADM-03 (knowledge list with filters), ADM-04 (edit and approve knowledge), and ADM-05 (reject/delete knowledge). Uses the KnowledgeItem model created in Plan 01.
Output: Working knowledge review page with server-side filtering, pagination, bulk operations, and inline editing
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-web-admin-panel/06-CONTEXT.md
@.planning/phases/06-web-admin-panel/06-PATTERNS.md
@.planning/phases/06-web-admin-panel/06-VALIDATION.md

From src/models/knowledge_item.py (created in Plan 01):
```python
class KnowledgeStatus(str, enum.Enum):
    PUBLISHED = "published"
    PENDING = "pending"
    REJECTED = "rejected"

class KnowledgeItem(Base):
    __tablename__ = "knowledge_items"
    id: Mapped[int]
    fact: Mapped[str]  # Text
    topic: Mapped[str]  # String(256)
    confidence: Mapped[float]  # Float
    source_refs: Mapped[str]  # Text, JSON string of SourceRef[]
    status: Mapped[KnowledgeStatus]
    created_at: Mapped[datetime]
    updated_at: Mapped[datetime]
```

From src/ai/extraction/schemas.py:
```python
class SourceRef(BaseModel):
    source_id: str
    excerpt: str
    timestamp: str | None = None
    page: int | None = None

class KnowledgeUnit(BaseModel):
    fact: str
    topic: str
    source_refs: list[SourceRef]
    confidence: float
```
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build knowledge review page with filters, pagination, and CRUD operations</name>
  <files>
    src/api/routes/admin.py,
    src/api/templates/knowledge/review.html,
    src/api/templates/knowledge/_edit_modal.html,
    src/api/templates/knowledge/_row.html,
    tests/test_admin.py
  </files>
  <read_first>
    - src/api/routes/admin.py (created in Plan 01, will extend)
    - src/models/knowledge_item.py (created in Plan 01, KnowledgeItem model)
    - src/models/__init__.py (verify KnowledgeItem export)
    - src/ai/extraction/schemas.py (SourceRef JSON structure for source_refs field)
    - src/core/config.py (knowledge_confidence_threshold for auto-publish)
    - tests/test_admin.py (extend with knowledge tests)
    - .planning/phases/06-web-admin-panel/06-PATTERNS.md (knowledge review table pattern reference)
  </read_first>
  <behavior>
    - Test 1: GET /api/admin/knowledge returns HTML with knowledge table
    - Test 2: GET /api/admin/knowledge?status=pending returns only pending items
    - Test 3: GET /api/admin/knowledge?topic=some_topic returns filtered items
    - Test 4: POST /api/admin/knowledge/approve with item_ids changes status to published
    - Test 5: POST /api/admin/knowledge/reject with item_ids changes status to rejected
    - Test 6: DELETE /api/admin/knowledge/{id} removes item from DB
    - Test 7: PUT /api/admin/knowledge/{id} with new fact text updates the fact field
    - Test 8: Approve/reject with non-existent IDs returns appropriate error
    - Test 9: Pagination: page=1 with limit=5 returns at most 5 items
  </behavior>
  <action>

**Step 1: Add knowledge endpoints to `src/api/routes/admin.py`**

Add these imports at the top of admin.py:
```python
from sqlalchemy import func
```

Add knowledge list endpoint:
```python
@router.get("/knowledge", response_class=HTMLResponse)
async def knowledge_page(
    request: Request,
    db: Session = Depends(get_db_session),
    status: str | None = None,
    topic: str | None = None,
    page: int = 1,
    limit: int = 20,
):
    query = db.query(KnowledgeItem)
    total = query.count()

    if status and status in ("published", "pending", "rejected"):
        query = query.filter(KnowledgeItem.status == status)
    if topic:
        query = query.filter(KnowledgeItem.topic.contains(topic))

    filtered_total = query.count()
    items = query.order_by(KnowledgeItem.created_at.desc()).offset((page - 1) * limit).limit(limit).all()

    # Get distinct topics for filter dropdown
    topics = db.query(KnowledgeItem.topic).distinct().all()

    status_counts = {
        "total": total,
        "published": db.query(KnowledgeItem).filter(KnowledgeItem.status == KnowledgeStatus.PUBLISHED).count(),
        "pending": db.query(KnowledgeItem).filter(KnowledgeItem.status == KnowledgeStatus.PENDING).count(),
        "rejected": db.query(KnowledgeItem).filter(KnowledgeItem.status == KnowledgeStatus.REJECTED).count(),
    }

    total_pages = max(1, (filtered_total + limit - 1) // limit)

    return templates.TemplateResponse(
        "knowledge/review.html",
        {
            "request": request,
            "items": items,
            "topics": [t[0] for t in topics],
            "current_status": status,
            "current_topic": topic,
            "current_page": page,
            "total_pages": total_pages,
            "filtered_total": filtered_total,
            "status_counts": status_counts,
        },
    )
```

Add approve endpoint:
```python
@router.post("/knowledge/approve", response_class=HTMLResponse)
async def approve_knowledge(
    request: Request,
    item_ids: list[int] = Form(...),
    db: Session = Depends(get_db_session),
):
    try:
        items = db.query(KnowledgeItem).filter(KnowledgeItem.id.in_(item_ids)).all()
        count = 0
        for item in items:
            item.status = KnowledgeStatus.PUBLISHED
            item.updated_at = datetime.utcnow()
            count += 1
        db.commit()
        return HTMLResponse(
            content=f'<div class="bg-green-50 text-green-700 px-4 py-3 rounded-lg text-sm">'
                    f'<i class="ti ti-check mr-1"></i> {count} знаний опубликовано</div>',
        )
    except Exception:
        db.rollback()
        return HTMLResponse(
            content='<div class="bg-red-50 text-red-700 px-4 py-3 rounded-lg text-sm">'
                    '<i class="ti ti-alert-circle mr-1"></i> Ошибка при публикации</div>',
            status_code=500,
        )
```

Add reject endpoint:
```python
@router.post("/knowledge/reject", response_class=HTMLResponse)
async def reject_knowledge(
    request: Request,
    item_ids: list[int] = Form(...),
    db: Session = Depends(get_db_session),
):
    try:
        items = db.query(KnowledgeItem).filter(KnowledgeItem.id.in_(item_ids)).all()
        count = 0
        for item in items:
            item.status = KnowledgeStatus.REJECTED
            item.updated_at = datetime.utcnow()
            count += 1
        db.commit()
        return HTMLResponse(
            content=f'<div class="bg-yellow-50 text-yellow-700 px-4 py-3 rounded-lg text-sm">'
                    f'<i class="ti ti-x mr-1"></i> {count} знаний отклонено</div>',
        )
    except Exception:
        db.rollback()
        return HTMLResponse(
            content='<div class="bg-red-50 text-red-700 px-4 py-3 rounded-lg text-sm">'
                    '<i class="ti ti-alert-circle mr-1"></i> Ошибка при отклонении</div>',
            status_code=500,
        )
```

Add delete endpoint:
```python
@router.delete("/knowledge/{item_id}", response_class=HTMLResponse)
async def delete_knowledge(item_id: int, db: Session = Depends(get_db_session)):
    item = db.query(KnowledgeItem).filter(KnowledgeItem.id == item_id).first()
    if not item:
        return HTMLResponse(
            content='<div class="bg-red-50 text-red-700 p-3 rounded">Знание не найдено</div>',
            status_code=404,
        )
    db.delete(item)
    db.commit()
    return HTMLResponse(content='<div class="bg-green-50 text-green-700 p-3 rounded">Знание удалено</div>')
```

Add edit endpoint:
```python
@router.put("/knowledge/{item_id}", response_class=HTMLResponse)
async def edit_knowledge(
    item_id: int,
    fact: str = Form(...),
    db: Session = Depends(get_db_session),
):
    item = db.query(KnowledgeItem).filter(KnowledgeItem.id == item_id).first()
    if not item:
        return HTMLResponse(
            content='<div class="bg-red-50 text-red-700 p-3 rounded">Знание не найдено</div>',
            status_code=404,
        )
    item.fact = fact
    item.updated_at = datetime.utcnow()
    db.commit()
    return templates.TemplateResponse(
        "knowledge/_row.html",
        {"request": Request(scope={"type": "http", "method": "GET", "path": "/"}), "item": item},
    )
```

**Step 2: Create `src/api/templates/knowledge/review.html`**

```html
{% extends "base.html" %}
{% block title %}Знания - V-Brain Admin{% endblock %}
{% block content %}
<div x-data="{ selectedItems: [] }">
    <div class="flex justify-between items-center mb-6">
        <h2 class="display">Проверка знаний</h2>
        <div class="flex gap-2 items-center">
            <!-- Bulk actions -->
            <template x-if="selectedItems.length > 0">
                <span class="text-sm text-gray-500" x-text="`${selectedItems.length} выбрано`"></span>
            </template>
            <form hx-post="/api/admin/knowledge/approve" hx-target="#bulk-result" hx-swap="innerHTML"
                  x-show="selectedItems.length > 0" class="inline">
                <template x-for="id in selectedItems"><input type="hidden" name="item_ids" :value="id"></template>
                <button type="submit"
                        class="bg-green-600 text-white px-3 py-1.5 rounded hover:bg-green-700 text-sm">
                    Одобрить
                </button>
            </form>
            <form hx-post="/api/admin/knowledge/reject" hx-target="#bulk-result" hx-swap="innerHTML"
                  x-show="selectedItems.length > 0" class="inline">
                <template x-for="id in selectedItems"><input type="hidden" name="item_ids" :value="id"></template>
                <button type="submit"
                        class="bg-yellow-500 text-white px-3 py-1.5 rounded hover:bg-yellow-600 text-sm">
                    Отклонить
                </button>
            </form>
        </div>
    </div>
    <div id="bulk-result"></div>

    <!-- Status summary cards -->
    <div class="grid grid-cols-4 gap-4 mb-6">
        <div class="bg-white rounded-lg shadow p-4">
            <div class="text-sm text-gray-500">Всего</div>
            <div class="text-2xl font-bold">{{ status_counts.total }}</div>
        </div>
        <div class="bg-white rounded-lg shadow p-4">
            <div class="text-sm text-gray-500">Опубликовано</div>
            <div class="text-2xl font-bold text-green-600">{{ status_counts.published }}</div>
        </div>
        <div class="bg-white rounded-lg shadow p-4">
            <div class="text-sm text-gray-500">На проверке</div>
            <div class="text-2xl font-bold text-yellow-600">{{ status_counts.pending }}</div>
        </div>
        <div class="bg-white rounded-lg shadow p-4">
            <div class="text-sm text-gray-500">Отклонено</div>
            <div class="text-2xl font-bold text-red-600">{{ status_counts.rejected }}</div>
        </div>
    </div>

    <!-- Filters -->
    <div class="flex gap-3 mb-4">
        <a href="/api/admin/knowledge"
           class="px-3 py-1.5 rounded text-sm {% if not current_status %}bg-blue-600 text-white{% else %}bg-white text-gray-700 hover:bg-gray-100{% endif %}">
            Все
        </a>
        <a href="/api/admin/knowledge?status=pending"
           class="px-3 py-1.5 rounded text-sm {% if current_status == 'pending' %}bg-blue-600 text-white{% else %}bg-white text-gray-700 hover:bg-gray-100{% endif %}">
            На проверке
        </a>
        <a href="/api/admin/knowledge?status=published"
           class="px-3 py-1.5 rounded text-sm {% if current_status == 'published' %}bg-blue-600 text-white{% else %}bg-white text-gray-700 hover:bg-gray-100{% endif %}">
            Опубликовано
        </a>
        <a href="/api/admin/knowledge?status=rejected"
           class="px-3 py-1.5 rounded text-sm {% if current_status == 'rejected' %}bg-blue-600 text-white{% else %}bg-white text-gray-700 hover:bg-gray-100{% endif %}">
            Отклонено
        </a>
        {% if topics %}
        <select onchange="window.location.href = '/api/admin/knowledge?status={{ current_status or '' }}&topic=' + this.value"
                class="px-3 py-1.5 rounded text-sm bg-white border">
            <option value="">Все темы</option>
            {% for topic in topics %}
            <option value="{{ topic }}" {% if current_topic == topic %}selected{% endif %}>{{ topic }}</option>
            {% endfor %}
        </select>
        {% endif %}
    </div>

    <!-- Knowledge table -->
    {% if not items %}
    <div class="bg-white rounded-lg shadow p-8 text-center text-gray-500">
        <p>Нет знаний для отображения</p>
    </div>
    {% else %}
    <div class="bg-white rounded-lg shadow overflow-hidden" id="knowledge-table-wrapper">
        <table class="w-full">
            <thead>
                <tr class="border-b bg-gray-50">
                    <th class="py-3 px-4 w-10">
                        <input type="checkbox" @change="
                            selectedItems = $el.checked
                                ? Array.from(document.querySelectorAll('.knowledge-checkbox')).map(cb => parseInt(cb.value))
                                : []">
                    </th>
                    <th class="text-left py-3 px-4 text-sm font-medium text-gray-600">Факт</th>
                    <th class="text-left py-3 px-4 text-sm font-medium text-gray-600">Тема</th>
                    <th class="text-left py-3 px-4 text-sm font-medium text-gray-600 w-24">Достоверность</th>
                    <th class="text-left py-3 px-4 text-sm font-medium text-gray-600 w-28">Статус</th>
                    <th class="text-left py-3 px-4 text-sm font-medium text-gray-600 w-32">Действия</th>
                </tr>
            </thead>
            <tbody>
                {% for item in items %}
                <tr class="border-b hover:bg-gray-50">
                    <td class="py-3 px-4">
                        <input type="checkbox"
                               class="knowledge-checkbox"
                               value="{{ item.id }}"
                               @change="
                                   const val = parseInt($el.value);
                                   if ($el.checked) selectedItems.push(val);
                                   else selectedItems = selectedItems.filter(i => i !== val);
                               ">
                    </td>
                    <td class="py-3 px-4">
                        <div class="max-w-xs truncate text-sm" title="{{ item.fact }}">{{ item.fact }}</div>
                    </td>
                    <td class="py-3 px-4">
                        <span class="px-2 py-1 rounded text-xs bg-gray-100 text-gray-700">{{ item.topic }}</span>
                    </td>
                    <td class="py-3 px-4">
                        <div class="flex items-center gap-2">
                            <div class="w-16 bg-gray-200 rounded-full h-1.5">
                                <div class="bg-blue-600 h-1.5 rounded-full"
                                     style="width: {{ (item.confidence * 100)|int }}%"></div>
                            </div>
                            <span class="text-xs text-gray-600">{{ (item.confidence * 100)|int }}%</span>
                        </div>
                    </td>
                    <td class="py-3 px-4">
                        <span class="px-2 py-1 rounded text-xs
                            {% if item.status.value == 'published' %}bg-green-100 text-green-800
                            {% elif item.status.value == 'pending' %}bg-yellow-100 text-yellow-800
                            {% else %}bg-red-100 text-red-800{% endif %}">
                            {% if item.status.value == 'published' %}Опубликовано
                            {% elif item.status.value == 'pending' %}На проверке
                            {% else %}Отклонено{% endif %}
                        </span>
                    </td>
                    <td class="py-3 px-4">
                        <div class="flex gap-1">
                            {% if item.status.value == 'pending' %}
                            <button hx-post="/api/admin/knowledge/approve"
                                    hx-vals='{"item_ids": [{{ item.id }}]}'
                                    hx-target="#row-{{ item.id }}"
                                    hx-swap="outerHTML"
                                    class="p-1.5 text-green-600 hover:bg-green-50 rounded" title="Одобрить">
                                <i class="ti ti-check"></i>
                            </button>
                            <button hx-post="/api/admin/knowledge/reject"
                                    hx-vals='{"item_ids": [{{ item.id }}]}'
                                    hx-target="#row-{{ item.id }}"
                                    hx-swap="outerHTML"
                                    class="p-1.5 text-yellow-600 hover:bg-yellow-50 rounded" title="Отклонить">
                                <i class="ti ti-x"></i>
                            </button>
                            {% endif %}
                            <button @click="document.getElementById('edit-modal').classList.remove('hidden'); document.getElementById('edit-fact').value = `{{ item.fact }}`; document.getElementById('edit-item-id').value = {{ item.id }};"
                                    class="p-1.5 text-blue-600 hover:bg-blue-50 rounded" title="Редактировать">
                                <i class="ti ti-edit"></i>
                            </button>
                            <button hx-delete="/api/admin/knowledge/{{ item.id }}"
                                    hx-target="#row-{{ item.id }}"
                                    hx-swap="outerHTML"
                                    class="p-1.5 text-red-600 hover:bg-red-50 rounded"
                                    onclick="return confirm('Delete knowledge: This will permanently remove this knowledge item')"
                                    title="Удалить">
                                <i class="ti ti-trash"></i>
                            </button>
                        </div>
                    </td>
                </tr>
                {% endfor %}
            </tbody>
        </table>
    </div>

    <!-- Pagination -->
    {% if total_pages > 1 %}
    <div class="flex justify-center gap-2 mt-4">
        {% if current_page > 1 %}
        <a href="/api/admin/knowledge?status={{ current_status or '' }}&topic={{ current_topic or '' }}&page={{ current_page - 1 }}"
           class="px-3 py-1.5 rounded text-sm bg-white hover:bg-gray-100">&larr; Назад</a>
        {% endif %}
        <span class="px-3 py-1.5 text-sm text-gray-500">Страница {{ current_page }} из {{ total_pages }}</span>
        {% if current_page < total_pages %}
        <a href="/api/admin/knowledge?status={{ current_status or '' }}&topic={{ current_topic or '' }}&page={{ current_page + 1 }}"
           class="px-3 py-1.5 rounded text-sm bg-white hover:bg-gray-100">Далее &rarr;</a>
        {% endif %}
    </div>
    {% endif %}
    {% endif %}

    <!-- Edit modal (hidden by default) -->
    <div id="edit-modal" class="hidden fixed inset-0 bg-black/50 flex items-center justify-center z-50"
         @click.self="$el.classList.add('hidden')">
        <div class="bg-white rounded-lg shadow-xl p-6 w-[500px]">
            <h3 class="mb-4">Редактировать знание</h3>
            <form hx-put="/api/admin/knowledge/" hx-target="#edit-result" hx-swap="innerHTML">
                <input type="hidden" id="edit-item-id" name="item_id">
                <textarea id="edit-fact" name="fact" rows="5"
                          class="w-full px-3 py-2 border rounded focus:outline-none focus:ring-2 focus:ring-blue-500 text-sm"></textarea>
                <div class="flex justify-end gap-2 mt-4">
                    <button type="button" @click="$el.closest('#edit-modal').classList.add('hidden')"
                            class="px-4 py-2 text-sm text-gray-700 hover:bg-gray-100 rounded">Отмена</button>
                    <button type="submit"
                            class="px-4 py-2 text-sm bg-blue-600 text-white rounded hover:bg-blue-700">Сохранить</button>
                </div>
            </form>
            <div id="edit-result"></div>
        </div>
    </div>
</div>
{% endblock %}
```

IMPORTANT NOTE ON EDIT MODAL: The `hx-put` URL needs to include the item_id dynamically. Since HTMX does not support dynamic URL construction from hidden input in the standard way, use a two-step approach:
1. The edit button sets `document.getElementById('edit-item-id').value = {{ item.id }}`
2. Use JavaScript to update the form action before submit:

```html
<form id="edit-form"
      @submit.prevent="
          const id = document.getElementById('edit-item-id').value;
          document.getElementById('edit-form').setAttribute('hx-put', '/api/admin/knowledge/' + id);
          htmx.submit('edit-form');
      ">
```

**Step 3: Add knowledge tests to `tests/test_admin.py`**

```python
# --- ADM-03, ADM-04, ADM-05: Knowledge review tests ---


def _make_knowledge_item(db_session, id=1, fact="Test fact", topic="Test topic", confidence=0.8, status=KnowledgeStatus.PENDING):
    """Helper to create a mock KnowledgeItem for tests."""
    from datetime import datetime
    item = MagicMock()
    item.id = id
    item.fact = fact
    item.topic = topic
    item.confidence = confidence
    item.status = status
    item.source_refs = "[]"
    item.created_at = datetime.utcnow()
    item.updated_at = datetime.utcnow()
    return item


def test_knowledge_page_renders_with_items(admin_client, db_session):
    items = [_make_knowledge_item(db_session, id=1), _make_knowledge_item(db_session, id=2, topic="Other")]
    db_session.query.return_value.filter.return_value.order_by.return_value.offset.return_value.limit.return_value.all.return_value = items
    db_session.query.return_value.count.return_value = 2
    db_session.query.return_value.distinct.return_value.all.return_value = [("Test topic",), ("Other",)]
    db_session.query.return_value.filter.return_value.count.return_value = 2

    # Set up status count queries
    def mock_count_side_effect(*args, **kwargs):
        return 2
    db_session.query.return_value.filter.return_value.count.side_effect = [2, 1, 1, 0]

    response = admin_client.get("/api/admin/knowledge")
    assert response.status_code == 200
    assert "Test fact" in response.text or "Знания" in response.text


def test_knowledge_filter_by_status(admin_client, db_session):
    pending_item = _make_knowledge_item(db_session, id=1, status=KnowledgeStatus.PENDING)
    db_session.query.return_value.filter.return_value.order_by.return_value.offset.return_value.limit.return_value.all.return_value = [pending_item]
    db_session.query.return_value.count.return_value = 1
    db_session.query.return_value.distinct.return_value.all.return_value = []

    def mock_count(*args, **kwargs):
        return 1
    db_session.query.return_value.filter.return_value.count.side_effect = [1, 0, 1, 0]

    response = admin_client.get("/api/admin/knowledge?status=pending")
    assert response.status_code == 200


def test_knowledge_approve_changes_status(admin_client, db_session):
    from src.models.knowledge_item import KnowledgeStatus
    item = _make_knowledge_item(db_session, id=1, status=KnowledgeStatus.PENDING)
    db_session.query.return_value.filter.return_value.all.return_value = [item]

    response = admin_client.post(
        "/api/admin/knowledge/approve",
        data={"item_ids": [1]},
    )
    assert response.status_code == 200
    assert "опубликовано" in response.text.lower()


def test_knowledge_reject_changes_status(admin_client, db_session):
    from src.models.knowledge_item import KnowledgeStatus
    item = _make_knowledge_item(db_session, id=1, status=KnowledgeStatus.PENDING)
    db_session.query.return_value.filter.return_value.all.return_value = [item]

    response = admin_client.post(
        "/api/admin/knowledge/reject",
        data={"item_ids": [1]},
    )
    assert response.status_code == 200
    assert "отклонено" in response.text.lower()


def test_knowledge_delete_removes_item(admin_client, db_session):
    item = _make_knowledge_item(db_session, id=1)
    db_session.query.return_value.filter.return_value.first.return_value = item

    response = admin_client.delete("/api/admin/knowledge/1")
    assert response.status_code == 200
    assert "удалено" in response.text.lower() or "deleted" in response.text.lower()


def test_knowledge_delete_not_found(admin_client, db_session):
    db_session.query.return_value.filter.return_value.first.return_value = None

    response = admin_client.delete("/api/admin/knowledge/999")
    assert response.status_code == 404
```

IMPORTANT: The mock setup for SQLAlchemy queries is complex because of the chained query pattern. Use `MagicMock()` for the db_session fixture and set up the chain: `db_session.query().filter().order_by().offset().limit().all()`. Each call returns the mock itself until `.all()` which returns the items list. Follow this pattern exactly:
```python
mock_query = MagicMock()
db_session.query.return_value = mock_query
mock_filter = MagicMock()
mock_query.filter.return_value = mock_filter
mock_filter.order_by.return_value = mock_filter
mock_filter.offset.return_value = mock_filter
mock_filter.limit.return_value = mock_filter
mock_filter.all.return_value = items
```

  </action>
  <verify>
    <automated>python -m pytest tests/test_admin.py -x -q --tb=short</automated>
  </verify>
  <done>
    - GET /api/admin/knowledge returns HTML table with knowledge items
    - Filtering by status (pending/published/rejected) works via query params
    - Filtering by topic works via query params
    - Approve endpoint changes item status to published
    - Reject endpoint changes item status to rejected
    - Delete endpoint removes item from database
    - Edit modal opens and submits PUT request to update fact text
    - Bulk approve/reject via checkboxes works
    - Pagination controls appear when more than 20 items exist
    - Status summary cards show total/published/pending/rejected counts
    - All tests pass including Plan 01 and Plan 02 tests
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser -> PUT /api/admin/knowledge/{id} | Fact text update from admin (trusted user) |
| browser -> POST /api/admin/knowledge/approve | Bulk approve action |
| browser -> POST /api/admin/knowledge/reject | Bulk reject action |
| browser -> DELETE /api/admin/knowledge/{id} | Delete action |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-6-10 | Tampering | Knowledge edit (PUT) | mitigate | Admin-authenticated endpoint (session required). Input is escaped by Jinja2 auto-escaping. Fact text stored in DB as-is. No risk since admin is the only user. |
| T-6-11 | Tampering | Bulk approve/reject | mitigate | Session-protected endpoint. Item IDs are integers validated by FastAPI Form parsing. Only items from DB can be modified. |
| T-6-12 | Information Disclosure | Knowledge fact display | accept | Knowledge contains anonymized data (PII removed by Phase 1 pipeline). No additional PII risk from displaying facts. |
</threat_model>

<verification>
1. `python -m pytest tests/test_admin.py -x -q --tb=short` passes (all tests including Plan 01 and Plan 02)
2. GET /api/admin/knowledge returns 200 with HTML containing knowledge table structure
3. Approve/reject endpoints change status and return success message
4. Delete endpoint removes item and returns success message
5. Edit modal form submits and returns updated row
</verification>

<success_criteria>
- Admin can view knowledge items with filters by topic, source, status (ADM-03)
- Admin can approve knowledge items (single and bulk) (ADM-04)
- Admin can edit knowledge fact text before approval (ADM-04)
- Admin can reject knowledge items (single and bulk) (ADM-05)
- Admin can delete knowledge items (ADM-05)
- Pagination works for large datasets
- All tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/06-web-admin-panel/06-03-SUMMARY.md`
</output>
