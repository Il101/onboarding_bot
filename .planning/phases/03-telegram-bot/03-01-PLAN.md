---
phase: 03-telegram-bot
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/core/config.py
  - src/ai/langgraph/state.py
  - src/models/feedback_event.py
  - src/bot/auth.py
  - src/bot/feedback.py
  - tests/phase3/test_graph_fallback.py
  - tests/phase3/test_source_block_always_present.py
  - tests/phase3/test_auth_gate.py
  - tests/phase3/test_multiturn_context.py
  - tests/phase3/test_feedback_capture.py
autonomous: true
requirements:
  - BOT-03
  - BOT-04
  - BOT-05
user_setup:
  - service: telegram
    why: "Telegram Bot API runtime access for Phase 3 handlers"
    env_vars:
      - name: TELEGRAM_BOT_TOKEN
        source: "BotFather -> created bot -> API token"
must_haves:
  truths:
    - "Состояние диалога и ответа бота валидируется строгими контрактами до отправки в Telegram."
    - "Проверка роли для /start выполняется детерминированно и даёт deny-решение до retrieval/LLM."
    - "Feedback thumbs up/down сохраняется как связанное событие с thread_id и message_id."
  artifacts:
    - path: "src/ai/langgraph/state.py"
      provides: "Typed BotState/BotAnswer/SourceRef contracts for graph and Telegram layer"
      exports: ["SourceRef", "BotAnswer", "BotState", "build_thread_id"]
      contains: "class BotAnswer"
    - path: "src/bot/auth.py"
      provides: "Pure authorization policy and deny payload factory for /start"
      exports: ["AuthDecision", "is_authorized_role", "build_access_denied_answer"]
      contains: "def is_authorized_role"
    - path: "src/models/feedback_event.py"
      provides: "Persistent feedback_events table model"
      contains: "__tablename__ = \"feedback_events\""
    - path: "src/bot/feedback.py"
      provides: "Feedback persistence service with explicit linkage fields"
      exports: ["save_feedback_event"]
      contains: "def save_feedback_event"
    - path: "tests/phase3/test_auth_gate.py"
      provides: "Regression test proving denied role never reaches retrieval"
  key_links:
    - from: "src/bot/auth.py"
      to: "src/ai/langgraph/state.py"
      via: "deny answer uses typed BotAnswer contract"
      pattern: "BotAnswer"
    - from: "src/bot/feedback.py"
      to: "src/models/feedback_event.py"
      via: "service writes FeedbackEvent rows with thread/message linkage"
      pattern: "FeedbackEvent\\("
    - from: "src/core/config.py"
      to: "src/bot/auth.py"
      via: "allowed roles and bot policy toggles are read from settings"
      pattern: "telegram_allowed_roles|telegram_feedback_enabled"
---

<objective>
Create Phase 3 interface and persistence foundation for bot policy enforcement before graph/transport wiring.

Purpose: Lock contracts and storage boundaries so downstream plans implement BOT-03/BOT-04/BOT-05 without interpretation drift.
Output: Phase 3 pytest scaffolds, typed bot state contracts, auth gate policy module, and feedback persistence foundation.
</objective>

<execution_context>
@.github/get-shit-done/workflows/execute-plan.md
@.github/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-telegram-bot/03-CONTEXT.md
@.planning/phases/03-telegram-bot/03-AI-SPEC.md
@.planning/phases/03-telegram-bot/03-RESEARCH.md
@.planning/phases/03-telegram-bot/03-PATTERNS.md
@.planning/phases/02-knowledge-extraction-rag/02-03-SUMMARY.md

<interfaces>
From src/api/routes/knowledge.py:
```python
class KnowledgeQueryRequest(BaseModel):
    query: str = Field(min_length=1)
    top_k: int | None = Field(default=None, ge=1, le=settings.rag_hybrid_top_k)
```

From src/core/config.py:
```python
class Settings(BaseSettings):
    rag_relevance_threshold: float = 0.35
    rag_hybrid_top_k: int = 6
```

From src/models/ingest_job.py:
```python
class IngestJob(Base):
    __tablename__ = "ingest_jobs"
    created_at: Mapped[datetime] = mapped_column(DateTime, default=datetime.utcnow)
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Phase 3 test harness and requirement-mapped scenario files</name>
  <files>tests/phase3/test_graph_fallback.py, tests/phase3/test_source_block_always_present.py, tests/phase3/test_auth_gate.py, tests/phase3/test_multiturn_context.py, tests/phase3/test_feedback_capture.py</files>
  <action>Create Phase 3 pytest files with deterministic fixtures/mocks and explicit assertions for BOT-01..BOT-05 policy behavior, including exact fallback phrase per D-01, mandatory `Источники` block per D-04/D-05, auth deny gate per D-11, one-clarify limit per D-08, and feedback persistence per D-12. Keep tests independent from Telegram network by patching clients/handlers and using in-memory doubles.</action>
  <verify>
    <automated>uv run pytest tests/phase3 --collect-only -q</automated>
  </verify>
  <done>All Phase 3 test modules exist, import correctly, and encode locked decisions D-01..D-12 as executable checks.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement state contracts and auth gate policy primitives</name>
  <files>src/core/config.py, src/ai/langgraph/state.py, src/bot/auth.py, tests/phase3/test_auth_gate.py, tests/phase3/test_multiturn_context.py</files>
  <behavior>
    - Test 1: `is_authorized_role` denies non-employee roles before any retrieval call (D-11).
    - Test 2: `BotAnswer` validation enforces non-empty sources list and RU-ready answer envelope (D-02, D-04, D-05).
    - Test 3: `build_thread_id` is deterministic per chat/user and reused across turns (D-06).
    - Test 4: settings load bot-specific fields (`telegram_bot_token`, `telegram_allowed_roles`, context budgets) with env overrides.
  </behavior>
  <action>Implement contracts and pure policy helpers first: extend `Settings` with Telegram/LangGraph runtime flags, create `SourceRef`, `BotAnswer`, `BotState`, and thread-id helper in `src/ai/langgraph/state.py`, then implement `src/bot/auth.py` with explicit `AuthDecision` reason codes. Enforce role gate per D-11 and keep denial payload deterministic (no free-form LLM response).</action>
  <verify>
    <automated>uv run pytest tests/phase3/test_auth_gate.py tests/phase3/test_multiturn_context.py -q</automated>
  </verify>
  <done>Typed state + auth modules are complete, tested, and ready for graph/telegram wiring without contract ambiguity.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add feedback persistence model and save service</name>
  <files>src/models/feedback_event.py, src/bot/feedback.py, tests/phase3/test_feedback_capture.py</files>
  <behavior>
    - Test 1: thumbs callback maps to normalized vote enum (`up`/`down`) and rejects unknown payload.
    - Test 2: save service persists `thread_id`, `message_id`, `vote`, `answer_confidence`, `created_at` (D-12).
    - Test 3: persistence keeps user/chat identifiers required for analytics joins without storing raw answer text.
  </behavior>
  <action>Create SQLAlchemy `FeedbackEvent` model and feedback save function using existing DB session pattern from `src/api/deps.py`; persist strictly the fields required by D-12 analytics. Include payload validation in `src/bot/feedback.py` to block callback tampering and return explicit failure for malformed callback data.</action>
  <verify>
    <automated>uv run pytest tests/phase3/test_feedback_capture.py -q</automated>
  </verify>
  <done>Feedback capture storage path exists with deterministic schema and automated coverage for valid/invalid callback events.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Telegram user input -> auth policy | Untrusted chat/user metadata crosses into authorization logic |
| callback payload -> feedback persistence | User-controlled callback_data crosses into DB write path |
| graph state -> storage contracts | Stateful conversation data crosses serialization/validation boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | E | `src/bot/auth.py` | mitigate | Validate role against allowlist from settings before graph retrieval node invocation; deny early with deterministic policy answer. |
| T-03-02 | T | `src/bot/feedback.py` | mitigate | Validate callback payload format and vote enum before DB write; reject malformed or replayed payloads. |
| T-03-03 | I | `src/ai/langgraph/state.py` | mitigate | Enforce Pydantic field constraints and source requirements to prevent sending ungrounded response envelopes. |
</threat_model>

<verification>
- `uv run pytest tests/phase3 --collect-only -q`
- `uv run pytest tests/phase3/test_auth_gate.py tests/phase3/test_feedback_capture.py -q`
</verification>

<success_criteria>
- BOT-05 auth gate primitives are implemented and testable before graph wiring.
- BOT-03 conversation state contracts are explicit and deterministic.
- BOT-04 feedback persistence schema/service exists and passes automated tests.
</success_criteria>

<output>
After completion, create `.planning/phases/03-telegram-bot/03-01-SUMMARY.md`
</output>
