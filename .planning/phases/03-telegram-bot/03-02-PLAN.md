---
phase: 03-telegram-bot
plan: 02
type: execute
wave: 2
depends_on:
  - 03-01
files_modified:
  - src/ai/langgraph/nodes/retrieve_phase2.py
  - src/ai/langgraph/nodes/summarize.py
  - src/ai/langgraph/nodes/decide.py
  - src/ai/langgraph/nodes/answer.py
  - src/ai/langgraph/graph.py
  - tests/phase3/test_graph_fallback.py
  - tests/phase3/test_source_block_always_present.py
  - tests/phase3/test_multiturn_context.py
autonomous: true
requirements:
  - BOT-01
  - BOT-02
  - BOT-03
must_haves:
  truths:
    - "При низкой уверенности бот всегда возвращает точную фразу fallback и не генерирует совет."
    - "Бот работает с контекстом multi-turn: история хранится по thread_id, старые сообщения суммаризуются."
    - "Для двусмысленного запроса бот задаёт ровно один уточняющий вопрос перед финальным ответом."
    - "При конфликте источников помечается конфликт и приоритет получает более свежая инструкция."
    - "Off-topic запросы получают краткий отказ и возврат к рабочей теме."
  artifacts:
    - path: "src/ai/langgraph/graph.py"
      provides: "Compiled state graph with deterministic branch routing"
      exports: ["build_graph", "route_after_auth", "route_after_decide"]
      contains: "StateGraph"
    - path: "src/ai/langgraph/nodes/retrieve_phase2.py"
      provides: "Phase 2 knowledge API adapter"
      exports: ["retrieve_phase2_payload"]
      contains: "/api/knowledge/query"
    - path: "src/ai/langgraph/nodes/decide.py"
      provides: "Policy branch selection for fallback/clarify/answer/offtopic/conflict"
      exports: ["decide_next_action"]
      contains: "Я не знаю — обратитесь к коллеге"
    - path: "src/ai/langgraph/nodes/summarize.py"
      provides: "Dynamic context window + summary update logic"
      exports: ["summarize_history_if_needed"]
      contains: "trim_messages"
    - path: "src/ai/langgraph/nodes/answer.py"
      provides: "Russian 3-6 step answer synthesis from Phase 2 envelope"
      exports: ["compose_grounded_answer"]
      contains: "Источники"
  key_links:
    - from: "src/ai/langgraph/nodes/retrieve_phase2.py"
      to: "src/api/routes/knowledge.py"
      via: "httpx POST with KnowledgeQueryRequest envelope"
      pattern: "/api/knowledge/query"
    - from: "src/ai/langgraph/nodes/decide.py"
      to: "src/bot/auth.py"
      via: "auth decision branch blocks retrieve/answer paths"
      pattern: "is_authorized_role"
    - from: "src/ai/langgraph/nodes/summarize.py"
      to: "src/ai/langgraph/state.py"
      via: "summary and messages persisted by thread_id state"
      pattern: "summary|messages"
---

<objective>
Implement Phase 3 policy graph and decision nodes over existing Phase 2 RAG contract.

Purpose: Deliver deterministic BOT-01/BOT-02/BOT-03 behavior (fallback, clarify limit, multi-turn memory, off-topic/conflict policy) before Telegram transport integration.
Output: LangGraph nodes and graph wiring with full policy regression coverage.
</objective>

<execution_context>
@.github/get-shit-done/workflows/execute-plan.md
@.github/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-telegram-bot/03-CONTEXT.md
@.planning/phases/03-telegram-bot/03-AI-SPEC.md
@.planning/phases/03-telegram-bot/03-RESEARCH.md
@.planning/phases/03-telegram-bot/03-PATTERNS.md
@.planning/phases/03-telegram-bot/03-01-PLAN.md
@.planning/phases/02-knowledge-extraction-rag/02-03-SUMMARY.md

<interfaces>
From src/ai/langgraph/state.py (from 03-01):
```python
class BotState(MessagesState):
    user_id: str
    role: str
    query: str
    summary: str
    rag_payload: dict[str, Any]
    result: BotAnswer | None
```

From src/api/routes/knowledge.py:
```python
@router.post("/query")
async def query_knowledge(payload: KnowledgeQueryRequest):
    return {"answer": ..., "confidence": ..., "sources": ..., "fallback_used": ...}
```

From src/ai/rag/synthesizer.py:
```python
FALLBACK_MESSAGE = "Недостаточно данных для уверенного ответа. Проверьте ближайшие источники."
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement retrieve + summarize nodes for multi-turn context handling</name>
  <files>src/ai/langgraph/nodes/retrieve_phase2.py, src/ai/langgraph/nodes/summarize.py, tests/phase3/test_multiturn_context.py</files>
  <behavior>
    - Test 1: retrieve node calls `/api/knowledge/query` with bounded `top_k` from settings and returns strict envelope.
    - Test 2: summarize node trims old turns and preserves conversation meaning in `summary` field (D-07).
    - Test 3: same chat/user pair resolves to same thread state, different user/chat resolves isolated state (D-06).
    - Test 4: summarize node never removes latest user message required for current turn.
  </behavior>
  <action>Implement retrieval adapter and dynamic context window logic per D-06/D-07 using thread-scoped state. Reuse Phase 2 query contract exactly (answer/confidence/sources/fallback_used) and avoid duplicating retrieval logic inside bot nodes. Keep summarization deterministic and token-budget controlled (trim old turns first, retain summary + latest turns).</action>
  <verify>
    <automated>uv run pytest tests/phase3/test_multiturn_context.py -q</automated>
  </verify>
  <done>Graph can reliably fetch Phase 2 payload and maintain multi-turn context through summary-aware state updates.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement decision and answer nodes with strict policy branches</name>
  <files>src/ai/langgraph/nodes/decide.py, src/ai/langgraph/nodes/answer.py, tests/phase3/test_graph_fallback.py, tests/phase3/test_source_block_always_present.py</files>
  <behavior>
    - Test 1: low-confidence or fallback payload returns exact phrase `Я не знаю — обратитесь к коллеге` (D-01).
    - Test 2: successful answer is Russian and formatted as 3-6 short steps (D-02, D-03).
    - Test 3: ambiguous input triggers exactly one clarification question before final answer (D-08).
    - Test 4: source conflict marks conflict and prefers freshest source (D-09).
    - Test 5: off-topic input returns concise refusal and redirects to work topic (D-10).
    - Test 6: each branch output (answer/fallback/clarify/deny/off-topic) includes `Источники` data for presenter (D-04, D-05).
  </behavior>
  <action>Implement `decide_next_action` and `compose_grounded_answer` with deterministic branch order: auth-deny -> off-topic -> low-confidence fallback -> clarify -> grounded answer. Use D-01 exact phrase, D-02 Russian-only response contract, D-03 3-6 step format, D-08 single-clarify limiter, D-09 newest-source conflict flag, D-10 concise refusal. If research suggests alternative phrasing, keep locked text per D-01.</action>
  <verify>
    <automated>uv run pytest tests/phase3/test_graph_fallback.py tests/phase3/test_source_block_always_present.py -q</automated>
  </verify>
  <done>Policy nodes emit deterministic, decision-compliant outputs across all mandatory branch types.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Wire full LangGraph routing and checkpoint-safe execution paths</name>
  <files>src/ai/langgraph/graph.py, tests/phase3/test_graph_fallback.py, tests/phase3/test_multiturn_context.py</files>
  <behavior>
    - Test 1: graph route exits immediately on deny role (no retrieve call).
    - Test 2: graph route for low-confidence query ends at fallback without answer synthesis call.
    - Test 3: clarify route can run only once per user turn key and then must resolve to final branch.
    - Test 4: graph central error handling masks internals and emits safe failure envelope with sources placeholder.
  </behavior>
  <action>Compile `StateGraph` with explicit nodes and conditional edges reflecting Phase 3 branch policy. Use checkpointer-compatible invocation contract (`configurable.thread_id`) to preserve state continuity and prevent cross-user leakage. Add central exception logging and safe error response path; do not leak raw stack traces in graph result.</action>
  <verify>
    <automated>uv run pytest tests/phase3/test_graph_fallback.py tests/phase3/test_multiturn_context.py -q</automated>
  </verify>
  <done>LangGraph orchestration is complete and policy-safe, ready for Telegram handler integration.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Graph state -> retrieval API | Bot-controlled query context crosses network boundary to backend API |
| Retrieval payload -> decision node | Untrusted/partial backend payload crosses into policy-critical branching |
| Decision output -> user response | Policy branch output crosses from backend state to user-visible instruction |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-04 | T | `src/ai/langgraph/nodes/decide.py` | mitigate | Enforce deterministic branch order and exact fallback text check before any answer generation. |
| T-03-05 | I | `src/ai/langgraph/nodes/summarize.py` | mitigate | Keep per-thread isolation and bounded context trimming to prevent cross-user or stale-context leakage. |
| T-03-06 | D | `src/ai/langgraph/nodes/retrieve_phase2.py` | mitigate | Use request timeout/retry bounds and top_k limits to prevent expensive or hanging retrieval calls. |
| T-03-07 | R | `src/ai/langgraph/nodes/answer.py` | mitigate | Preserve source metadata and conflict markers in output model for post-hoc traceability. |
</threat_model>

<verification>
- `uv run pytest tests/phase3/test_graph_fallback.py tests/phase3/test_source_block_always_present.py tests/phase3/test_multiturn_context.py -q`
</verification>

<success_criteria>
- BOT-02 strict fallback policy is implemented with exact phrase and tested.
- BOT-03 multi-turn state and one-clarify behavior are enforced by graph logic.
- D-09 conflict and D-10 off-topic handling are deterministic and covered by tests.
</success_criteria>

<output>
After completion, create `.planning/phases/03-telegram-bot/03-02-SUMMARY.md`
</output>
