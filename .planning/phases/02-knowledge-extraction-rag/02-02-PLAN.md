---
phase: 02-knowledge-extraction-rag
plan: 02
type: execute
wave: 2
depends_on:
  - 02-01
files_modified:
  - src/ai/extraction/extractor.py
  - src/pipeline/indexer/knowledge_writer.py
  - src/ai/sop/template.py
  - src/ai/sop/generator.py
  - src/tasks/knowledge.py
  - tests/phase2/test_extraction_pipeline.py
  - tests/phase2/test_knowledge_indexing.py
  - tests/phase2/test_sop_generation.py
autonomous: true
requirements:
  - KNW-01
  - KNW-02
  - KNW-03
  - KNW-04

must_haves:
  truths:
    - "System extracts atomic structured knowledge units from anonymized chunks and groups by topic"
    - "Publishable extraction output is indexed into Qdrant as knowledge-unit records before online retrieval"
    - "Low-confidence knowledge is excluded from published output and marked for review"
    - "SOP markdown is generated only in fixed template: goal -> steps -> exceptions -> verification"
    - "Each generated SOP includes detailed source attribution entries"
  artifacts:
    - path: "src/ai/extraction/extractor.py"
      provides: "Extraction orchestration from chunk input to validated/publishable units"
      exports: ["extract_knowledge_units", "group_units_by_topic"]
      contains: "def extract_knowledge_units"
    - path: "src/pipeline/indexer/knowledge_writer.py"
      provides: "Index writer that maps publishable knowledge units into Qdrant points/payload"
      exports: ["index_knowledge_units"]
      contains: "def index_knowledge_units"
    - path: "src/ai/sop/template.py"
      provides: "Deterministic SOP section template"
      exports: ["SOP_SECTION_ORDER", "render_sop"]
      contains: "SOP_SECTION_ORDER"
    - path: "src/ai/sop/generator.py"
      provides: "SOP assembly from validated knowledge clusters with attribution"
      exports: ["generate_sop_for_topic"]
      contains: "def generate_sop_for_topic"
    - path: "src/tasks/knowledge.py"
      provides: "Celery orchestration for extraction and SOP generation jobs"
      exports: ["extract_knowledge_task", "generate_sop_task"]
      contains: "def extract_knowledge_task"
  key_links:
    - from: "src/tasks/knowledge.py"
      to: "src/ai/extraction/extractor.py"
      via: "task invokes extraction stage and policy gate"
      pattern: "extract_knowledge_units"
    - from: "src/ai/extraction/extractor.py"
      to: "src/pipeline/indexer/knowledge_writer.py"
      via: "publishable units are passed to index writer for Qdrant upsert"
      pattern: "index_knowledge_units"
    - from: "src/pipeline/indexer/knowledge_writer.py"
      to: "src/pipeline/indexer/qdrant_store.py"
      via: "writer uses QdrantStore collection contract for persisted knowledge units"
      pattern: "COLLECTION_NAME\\s*=\\s*\"knowledge\""
    - from: "src/ai/sop/generator.py"
      to: "src/ai/sop/template.py"
      via: "generator renders fixed template order"
      pattern: "render_sop"
    - from: "src/ai/sop/generator.py"
      to: "src/ai/rag/contracts.py"
      via: "SOP attribution entries follow typed contract"
      pattern: "AttributionItem"
---

<objective>
Implement offline knowledge extraction and SOP generation pipeline using strict contracts from Plan 02-01.

Purpose: Deliver KNW-01 and KNW-02 while completing KNW-03 indexing prerequisites (publishable extraction output persisted in Qdrant) with attribution traceability for downstream bot/admin phases.
Output: Extraction service, Qdrant knowledge index writer, SOP generator/template, and Celery tasks for asynchronous execution.
</objective>

<execution_context>
@.github/get-shit-done/workflows/execute-plan.md
@.github/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-knowledge-extraction-rag/02-CONTEXT.md
@.planning/phases/02-knowledge-extraction-rag/02-RESEARCH.md
@.planning/phases/02-knowledge-extraction-rag/02-PATTERNS.md
@.planning/phases/02-knowledge-extraction-rag/02-AI-SPEC.md
@.planning/phases/02-knowledge-extraction-rag/02-01-PLAN.md

<interfaces>
From src/ai/extraction/schemas.py (created in 02-01):
```python
class SourceRef(BaseModel): ...
class KnowledgeUnit(BaseModel): ...
class KnowledgeBatch(BaseModel): ...
```

From src/ai/extraction/publish_policy.py (created in 02-01):
```python
def should_publish_knowledge(unit: KnowledgeUnit) -> PublishDecision: ...
```

From src/pipeline/indexer/qdrant_store.py:
```python
class QdrantStore:
    COLLECTION_NAME = "knowledge"
```

From src/tasks/ingest.py:
```python
@celery_app.task(bind=True, autoretry_for=(Exception,), max_retries=3)
def ingest_telegram(...): ...
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build structured extraction pipeline with topic grouping and confidence gate (D-01..D-03, D-06, D-07)</name>
  <files>src/ai/extraction/extractor.py, src/tasks/knowledge.py, tests/phase2/test_extraction_pipeline.py</files>
  <read_first>
    @src/tasks/ingest.py
    @.planning/phases/02-knowledge-extraction-rag/02-PATTERNS.md
    @.planning/phases/02-knowledge-extraction-rag/02-01-PLAN.md
  </read_first>
  <behavior>
    - Test 1: extraction returns only schema-valid KnowledgeUnit items.
    - Test 2: one extracted unit maps to one atomic fact/rule (single-fact invariant).
    - Test 3: units below `knowledge_confidence_threshold` are routed to `review_needed` and excluded from `publishable`.
    - Test 4: grouping function clusters publishable units by `topic`.
    - Test 5: Celery task reports progress stages (`extracting`, `validating`, `gating`, `grouping`, `completed`).
  </behavior>
  <action>Create extractor orchestration that processes anonymized chunks into KnowledgeUnit batches, validates with schemas from 02-01, applies publish gate (D-06/D-07), and outputs both publishable and review-needed sets. Reuse Phase 1 Celery progress/error handling style in `src/tasks/knowledge.py`; do not bypass validation by passing raw LLM JSON directly to storage or indexing.</action>
  <acceptance_criteria>
    - Extraction path is deterministic and rejects invalid units.
    - Low-confidence items are blocked from publication and retained for review.
    - Topic grouping output is stable for identical input.
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest tests/phase2/test_extraction_pipeline.py -q</automated>
  </verify>
  <done>Extractor and task orchestration produce grouped publishable knowledge with explicit review queue output for low-confidence units.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Index publishable extraction output into Qdrant for retrieval consumption (D-08, D-09)</name>
  <files>src/pipeline/indexer/knowledge_writer.py, src/tasks/knowledge.py, tests/phase2/test_knowledge_indexing.py</files>
  <read_first>
    @src/pipeline/indexer/qdrant_store.py
    @src/pipeline/indexer/embedder.py
    @.planning/phases/02-knowledge-extraction-rag/02-RESEARCH.md
  </read_first>
  <behavior>
    - Test 1: index writer converts each publishable KnowledgeUnit into one Qdrant point with source/topic/confidence payload.
    - Test 2: writer uses `knowledge` collection contract and preserves source locator metadata needed for attribution.
    - Test 3: extraction Celery task calls index writer only for publishable units (never review-needed).
    - Test 4: empty publishable set performs no-op indexing and returns explicit indexed_count=0.
  </behavior>
  <action>Implement `index_knowledge_units` adapter that writes extraction outputs to Qdrant with payload schema compatible with downstream hybrid retrieval (D-08/D-09). Wire this writer into `src/tasks/knowledge.py` immediately after gating/grouping so the retrieval pipeline consumes extracted knowledge artifacts instead of raw ingestion chunks. Include deterministic test doubles for Qdrant upsert calls and assert metadata fidelity (`source_id`, `excerpt`, `timestamp/page`, `topic`, `confidence`).</action>
  <acceptance_criteria>
    - Publishable extraction results are physically indexed in Qdrant with deterministic payload shape.
    - Retriever-facing metadata fields required by KNW-04 are retained through index write.
    - Task telemetry reports indexed count for observability.
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest tests/phase2/test_knowledge_indexing.py -q</automated>
  </verify>
  <done>Extraction output indexing path is implemented and tested end-to-end at service boundary.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Generate SOP markdown from clustered knowledge with fixed template and attribution (D-04, D-05, D-10, D-11)</name>
  <files>src/ai/sop/template.py, src/ai/sop/generator.py, src/tasks/knowledge.py, tests/phase2/test_sop_generation.py</files>
  <read_first>
    @.planning/phases/02-knowledge-extraction-rag/02-CONTEXT.md
    @.planning/phases/02-knowledge-extraction-rag/02-AI-SPEC.md
    @src/pipeline/chunker/text_chunker.py
  </read_first>
  <behavior>
    - Test 1: generated SOP markdown always contains ordered sections: `## Цель`, `## Шаги`, `## Исключения`, `## Проверка результата`.
    - Test 2: generator does not emit free-form SOP without required sections.
    - Test 3: SOP payload includes attribution list with `source_id + excerpt + timestamp/page`.
    - Test 4: empty/insufficient publishable units return explicit non-generation result (not malformed SOP).
  </behavior>
  <action>Implement deterministic template renderer and SOP generator that consumes grouped publishable knowledge units (already indexed by Task 2, not raw chunks) per D-04/D-05. Add attribution block to SOP artifact with detailed source references per D-10/D-11. Extend `src/tasks/knowledge.py` with Celery SOP generation task that receives clustered units and emits markdown artifact + attribution metadata.</action>
  <acceptance_criteria>
    - SOP output strictly follows fixed structure and is machine-checkable.
    - Attribution in SOP is operator-verifiable and complete.
    - Generator handles insufficient input safely without hallucinated SOP.
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest tests/phase2/test_sop_generation.py -q</automated>
  </verify>
  <done>SOP generation path is fully implemented with fixed structure and attribution contract, integrated into Celery flow.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| chunk corpus -> extractor | Large unstructured text crosses into LLM/validation pipeline |
| extraction output -> SOP generator | Validated knowledge units become operational instruction artifacts |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-04 | T | `src/ai/extraction/extractor.py` | mitigate | Validate all extraction outputs against strict schema and reject malformed JSON before grouping/publish. |
| T-02-05 | I | `src/ai/sop/generator.py` | mitigate | Generate SOP only from publishable validated units; include attribution fields for every operational step reference. |
| T-02-06 | D | `src/tasks/knowledge.py` | mitigate | Use Celery retries/progress/failure states to prevent silent task stalls and provide recoverable async processing. |
</threat_model>

<verification>
- `uv run pytest tests/phase2/test_extraction_pipeline.py tests/phase2/test_knowledge_indexing.py tests/phase2/test_sop_generation.py -q`
- `uv run pytest tests/phase2 -q`
</verification>

<success_criteria>
- KNW-01 extraction pipeline is automated, validated, grouped by topic, and confidence-gated.
- KNW-03 ingestion side is complete: publishable extraction units are indexed into Qdrant with retriever-compatible payload.
- KNW-02 SOP generation is deterministic and follows fixed template structure.
- KNW-04 attribution data is present in SOP artifacts and ready for downstream consumption.
</success_criteria>

<output>
After completion, create `.planning/phases/02-knowledge-extraction-rag/02-02-SUMMARY.md`
</output>
