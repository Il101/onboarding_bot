---
phase: 02-knowledge-extraction-rag
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/core/config.py
  - src/ai/extraction/schemas.py
  - src/ai/extraction/publish_policy.py
  - src/ai/rag/contracts.py
  - tests/phase2/test_contracts_and_policy.py
autonomous: true
requirements:
  - KNW-01
  - KNW-03
  - KNW-04

must_haves:
  truths:
    - "Extraction output has strict JSON contract with required fields and typed source references"
    - "Confidence and relevance thresholds are configurable (not hardcoded) and enforce publish/query gates"
    - "RAG answer contract requires source attribution fields for operator verification"
  artifacts:
    - path: "src/ai/extraction/schemas.py"
      provides: "Strict knowledge-unit and source-reference contracts"
      exports: ["SourceRef", "KnowledgeUnit", "KnowledgeBatch"]
      contains: "class KnowledgeUnit"
    - path: "src/ai/extraction/publish_policy.py"
      provides: "Confidence-based publish gate for low-confidence blocking"
      exports: ["should_publish_knowledge", "PublishDecision"]
      contains: "def should_publish_knowledge"
    - path: "src/ai/rag/contracts.py"
      provides: "Typed response/attribution contracts for RAG output"
      exports: ["AttributionItem", "RagAnswer"]
      contains: "class AttributionItem"
    - path: "src/core/config.py"
      provides: "Phase-2 thresholds and retrieval tuning settings"
      contains: "knowledge_confidence_threshold"
  key_links:
    - from: "src/core/config.py"
      to: "src/ai/extraction/publish_policy.py"
      via: "knowledge_confidence_threshold read by publish gate"
      pattern: "settings\\.knowledge_confidence_threshold"
    - from: "src/ai/extraction/schemas.py"
      to: "src/ai/extraction/publish_policy.py"
      via: "publish policy consumes KnowledgeUnit.confidence"
      pattern: "KnowledgeUnit"
    - from: "src/ai/rag/contracts.py"
      to: "src/ai/rag/attribution.py"
      via: "formatter returns AttributionItem list"
      pattern: "AttributionItem"
---

<objective>
Establish Phase 2 interface contracts and policy gates before implementation.

Purpose: Freeze extraction/RAG contracts so downstream tasks implement deterministically and satisfy locked user decisions.
Output: Typed schemas, publish-policy gate, RAG response contract, and config thresholds with tests.
</objective>

<execution_context>
@.github/get-shit-done/workflows/execute-plan.md
@.github/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-knowledge-extraction-rag/02-CONTEXT.md
@.planning/phases/02-knowledge-extraction-rag/02-RESEARCH.md
@.planning/phases/02-knowledge-extraction-rag/02-PATTERNS.md
@.planning/phases/02-knowledge-extraction-rag/02-AI-SPEC.md
@.planning/phases/01-foundation-data-ingestion/01-06-SUMMARY.md

<interfaces>
From src/core/config.py:
```python
class Settings(BaseSettings):
    pii_confidence_threshold: float = 0.5
    telegram_grouping_window_minutes: int = 30
settings = Settings()
```

From src/pipeline/indexer/qdrant_store.py:
```python
class QdrantStore:
    COLLECTION_NAME = "knowledge"
```

From src/tasks/ingest.py:
```python
for chunk in chunks:
    chunk["metadata"]["source_id"] = source_id
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define extraction and RAG contracts (D-01, D-02, D-03, D-10, D-11)</name>
  <files>src/ai/extraction/schemas.py, src/ai/rag/contracts.py, tests/phase2/test_contracts_and_policy.py</files>
  <read_first>
    @.planning/phases/02-knowledge-extraction-rag/02-CONTEXT.md
    @.planning/phases/02-knowledge-extraction-rag/02-PATTERNS.md
    @src/models/source.py
  </read_first>
  <behavior>
    - Test 1: KnowledgeUnit rejects missing required fields (`fact`, `topic`, `source_refs`, `confidence`).
    - Test 2: KnowledgeUnit rejects confidence outside [0,1].
    - Test 3: source_refs require `source_id` and at least one locator (`timestamp` or `page`) for manual verification.
    - Test 4: RagAnswer contract requires attribution items with `source_id`, `excerpt`, and score.
  </behavior>
  <action>Implement strict Pydantic models for extraction outputs and RAG response payload, per D-01..D-03 and D-10..D-11. Keep one knowledge unit atomic (single fact/rule) per D-03; do not allow free-form untyped dict payloads. Add contract tests first (RED), then implement models (GREEN).</action>
  <acceptance_criteria>
    - Extraction contract is typed and fails fast on malformed data.
    - Attribution payload is structurally sufficient for manual operator traceability.
    - Tests demonstrate invalid payloads are rejected.
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest tests/phase2/test_contracts_and_policy.py::test_knowledge_unit_contract tests/phase2/test_contracts_and_policy.py::test_rag_answer_contract -q</automated>
  </verify>
  <done>Contract models and focused tests exist and pass; no `Any`-based payloads for extraction/RAG interfaces.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement publish/relevance gate configuration and policy (D-06, D-07, D-12, D-13)</name>
  <files>src/core/config.py, src/ai/extraction/publish_policy.py, tests/phase2/test_contracts_and_policy.py</files>
  <read_first>
    @src/core/config.py
    @.planning/phases/02-knowledge-extraction-rag/02-CONTEXT.md
    @.planning/phases/02-knowledge-extraction-rag/02-RESEARCH.md
  </read_first>
  <behavior>
    - Test 1: `should_publish_knowledge` blocks low-confidence unit below threshold.
    - Test 2: publish decision includes explicit reason when blocked.
    - Test 3: settings expose `knowledge_confidence_threshold` and `rag_relevance_threshold`.
    - Test 4: threshold defaults are conservative and can be overridden via env.
  </behavior>
  <action>Add Phase-2 settings in `src/core/config.py` (`knowledge_confidence_threshold`, `rag_relevance_threshold`, `rag_similarity_top_k`, `rag_sparse_top_k`, `rag_hybrid_top_k`, `rag_rerank_top_k`) and implement `publish_policy.py` to enforce low-confidence non-publication per D-06/D-07. Include explicit policy helper for low-relevance refusal path per D-12/D-13; do not hardcode thresholds outside settings.</action>
  <acceptance_criteria>
    - Confidence and relevance thresholds are config-driven and tested.
    - Low-confidence units are blocked with reason metadata for review queue.
    - No module bypasses gate by direct confidence comparison hardcoded inline.
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest tests/phase2/test_contracts_and_policy.py::test_publish_policy_threshold tests/phase2/test_contracts_and_policy.py::test_phase2_threshold_settings -q</automated>
  </verify>
  <done>Policy layer and settings are in place, with deterministic tests proving D-06/D-07/D-12/D-13 guardrails.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| extraction output -> policy gate | LLM-generated JSON crosses into publishable knowledge decisions |
| retrieval score -> answer policy | Numerical relevance signal gates whether system can provide operational answer |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | T | `src/ai/extraction/schemas.py` | mitigate | Enforce strict typed schema validation; reject malformed or missing attribution fields at parse boundary. |
| T-02-02 | R | `src/ai/rag/contracts.py` | mitigate | Require explicit attribution fields (`source_id`, locator, excerpt, score) to preserve non-repudiation of answer provenance. |
| T-02-03 | I | `src/ai/extraction/publish_policy.py` | mitigate | Block low-confidence units per threshold and return reason; prevent unreviewed publication path. |
</threat_model>

<verification>
- `uv run pytest tests/phase2/test_contracts_and_policy.py -q`
- `uv run pytest tests/ -q -k "contracts_and_policy or qdrant_indexer"`
</verification>

<success_criteria>
- KNW-01 contracts are strict and test-validated.
- KNW-04 attribution contract exists and is enforced at type level.
- D-06/D-07/D-12/D-13 thresholds are configurable and govern policy decisions.
</success_criteria>

<output>
After completion, create `.planning/phases/02-knowledge-extraction-rag/02-01-SUMMARY.md`
</output>

