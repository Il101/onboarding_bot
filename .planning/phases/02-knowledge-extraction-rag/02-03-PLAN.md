---
phase: 02-knowledge-extraction-rag
plan: 03
type: execute
wave: 3
depends_on:
  - 02-01
  - 02-02
files_modified:
  - src/ai/rag/retriever.py
  - src/ai/rag/reranker.py
  - src/ai/rag/attribution.py
  - src/ai/rag/synthesizer.py
  - src/api/routes/knowledge.py
  - src/api/main.py
  - tests/phase2/test_hybrid_retrieval.py
  - tests/phase2/test_attribution_contract.py
  - tests/phase2/test_low_relevance_fallback.py
autonomous: true
requirements:
  - KNW-03
  - KNW-04
  - KNW-01

must_haves:
  truths:
    - "RAG query path defaults to hybrid dense+sparse retrieval with reranking"
    - "When relevance is below threshold, API returns 'недостаточно данных' with nearest sources instead of fabricated answer"
    - "Every successful answer includes attribution fields with source_id, excerpt, and timestamp/page"
    - "Knowledge query API exposes deterministic response envelope for downstream bot/admin use"
  artifacts:
    - path: "src/ai/rag/retriever.py"
      provides: "Hybrid retrieval over Qdrant knowledge collection"
      exports: ["HybridRetriever"]
      contains: "vector_store_query_mode=\"hybrid\""
    - path: "src/ai/rag/reranker.py"
      provides: "Top-K reranking stage after retrieval"
      exports: ["rerank_candidates"]
      contains: "def rerank_candidates"
    - path: "src/ai/rag/synthesizer.py"
      provides: "Answer synthesis with low-relevance fallback gate"
      exports: ["synthesize_answer"]
      contains: "def synthesize_answer"
    - path: "src/ai/rag/attribution.py"
      provides: "Attribution formatter for response contract"
      exports: ["format_attribution"]
      contains: "def format_attribution"
    - path: "src/api/routes/knowledge.py"
      provides: "HTTP endpoints for retrieval query and async job status"
      exports: ["router"]
      contains: "@router.post(\"/query\")"
  key_links:
    - from: "src/ai/extraction/extractor.py"
      to: "src/pipeline/indexer/knowledge_writer.py"
      via: "publishable extraction units are transformed into Qdrant records"
      pattern: "index_knowledge_units"
    - from: "src/pipeline/indexer/knowledge_writer.py"
      to: "src/ai/rag/retriever.py"
      via: "retriever consumes indexed knowledge payload fields for hybrid search and attribution"
      pattern: "source_id|topic|confidence"
    - from: "src/ai/rag/retriever.py"
      to: "src/pipeline/indexer/qdrant_store.py"
      via: "shared collection contract (`knowledge`) and payload metadata"
      pattern: "COLLECTION_NAME\\s*=\\s*\"knowledge\""
    - from: "src/ai/rag/synthesizer.py"
      to: "src/core/config.py"
      via: "relevance threshold read from settings"
      pattern: "settings\\.rag_relevance_threshold"
    - from: "src/api/routes/knowledge.py"
      to: "src/ai/rag/synthesizer.py"
      via: "query endpoint runs retrieval->rerank->synthesis pipeline"
      pattern: "synthesize_answer"
---

<objective>
Deliver online RAG retrieval/synthesis path and API surface for Phase 2, with mandatory fallback and attribution guarantees.

Purpose: Complete KNW-03 and KNW-04 so downstream Telegram bot/admin can call stable knowledge query endpoints.
Output: Hybrid retriever, reranker, synthesizer, attribution formatter, and FastAPI knowledge routes with tests.
</objective>

<execution_context>
@.github/get-shit-done/workflows/execute-plan.md
@.github/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-knowledge-extraction-rag/02-CONTEXT.md
@.planning/phases/02-knowledge-extraction-rag/02-RESEARCH.md
@.planning/phases/02-knowledge-extraction-rag/02-PATTERNS.md
@.planning/phases/02-knowledge-extraction-rag/02-AI-SPEC.md
@.planning/phases/02-knowledge-extraction-rag/02-02-PLAN.md

<interfaces>
From src/pipeline/indexer/qdrant_store.py:
```python
class QdrantStore:
    COLLECTION_NAME = "knowledge"
```

From src/pipeline/indexer/knowledge_writer.py (created in 02-02):
```python
def index_knowledge_units(...): ...
```

From src/ai/rag/contracts.py (created in 02-01):
```python
class AttributionItem(BaseModel): ...
class RagAnswer(BaseModel): ...
```

From src/api/routes/ingest.py:
```python
router = APIRouter(prefix="/api/ingest", tags=["ingestion"])
@router.get("/status/{job_id}")
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement hybrid retrieval + reranking + attribution formatter (D-08, D-09, D-10, D-11)</name>
  <files>src/ai/rag/retriever.py, src/ai/rag/reranker.py, src/ai/rag/attribution.py, tests/phase2/test_hybrid_retrieval.py, tests/phase2/test_attribution_contract.py</files>
  <read_first>
    @src/pipeline/indexer/qdrant_store.py
    @src/pipeline/indexer/embedder.py
    @.planning/phases/02-knowledge-extraction-rag/02-RESEARCH.md
  </read_first>
  <behavior>
    - Test 1: retriever uses hybrid mode as default (not dense-only/sparse-only).
    - Test 2: retrieval result list is reranked and truncated to configured top-k.
    - Test 3: attribution formatter emits `source_id`, `excerpt`, and `timestamp/page` for each selected source.
    - Test 4: attribution formatter preserves original retrieval score for auditability.
  </behavior>
  <action>Implement retriever over existing Qdrant `knowledge` collection using hybrid dense+sparse retrieval per D-08/D-09. Add deterministic rerank stage (model or scoring heuristic per discretion) and formatter that maps selected nodes to attribution contract per D-10/D-11. Keep code path metadata-preserving; never drop source locator fields during transformation.</action>
  <acceptance_criteria>
    - Hybrid retrieval is explicit and test-proven.
    - Rerank stage is separately testable deterministic function.
    - Attribution payload is complete and operator-verifiable.
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest tests/phase2/test_hybrid_retrieval.py tests/phase2/test_attribution_contract.py -q</automated>
  </verify>
  <done>Retriever/reranker/attribution modules exist and pass contract-focused tests.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement synthesis fallback policy and expose knowledge query API (D-12, D-13)</name>
  <files>src/ai/rag/synthesizer.py, src/api/routes/knowledge.py, src/api/main.py, tests/phase2/test_low_relevance_fallback.py</files>
  <read_first>
    @src/api/routes/ingest.py
    @src/api/main.py
    @.planning/phases/02-knowledge-extraction-rag/02-CONTEXT.md
  </read_first>
  <behavior>
    - Test 1: when top relevance score < threshold, response answer equals fallback message and includes nearest sources.
    - Test 2: when relevance >= threshold, response includes grounded answer + attribution list.
    - Test 3: API `/api/knowledge/query` returns stable envelope (`answer`, `confidence`, `sources`, `fallback_used`).
    - Test 4: API rejects malformed query payload with 422/400 and no internal error leakage.
  </behavior>
  <action>Implement synthesizer that executes retrieval->rerank->policy gate->answer assembly. Enforce refusal path for low relevance per D-12/D-13 with message "Недостаточно данных..." and top nearest sources. Add `src/api/routes/knowledge.py` and register router in `src/api/main.py`; keep route style consistent with ingestion routes (validation, safe error envelope, async handler). Do not return answer without attribution fields.</action>
  <acceptance_criteria>
    - Low-relevance questions never produce fabricated operational guidance.
    - Knowledge query endpoint is callable and returns deterministic contract for downstream phases.
    - Fallback and success paths are both covered by automated tests.
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest tests/phase2/test_low_relevance_fallback.py -q</automated>
  </verify>
  <done>RAG synthesis and knowledge API are implemented with fallback and attribution guarantees.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user query -> retrieval pipeline | Untrusted free-text input enters retrieval/synthesis pipeline |
| retrieved context -> generated answer | Model output may deviate from evidence if not gated |
| API response -> downstream bot/admin | Downstream systems trust response schema and source attribution |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-07 | T | `src/ai/rag/synthesizer.py` | mitigate | Enforce relevance threshold gate and fallback message path; block answer generation under low relevance. |
| T-02-08 | I | `src/ai/rag/attribution.py` | mitigate | Emit complete attribution fields for each answer source and test contract coverage. |
| T-02-09 | D | `src/api/routes/knowledge.py` | mitigate | Validate payload shape and constrain top-k settings from config to prevent abusive high-cost queries. |
</threat_model>

<verification>
- `uv run pytest tests/phase2/test_hybrid_retrieval.py tests/phase2/test_attribution_contract.py tests/phase2/test_low_relevance_fallback.py -q`
- `uv run pytest tests/phase2 -v`
- `uv run pytest tests/ -v --tb=short`
</verification>

<success_criteria>
- KNW-03 implemented end-to-end: extraction output indexed into Qdrant (02-02) and consumed by hybrid retrieval + reranking as default online path.
- KNW-04 satisfied with complete attribution in every answer/fallback response.
- D-12/D-13 satisfied: low-relevance requests return explicit insufficient-data response plus nearest sources.
</success_criteria>

<output>
After completion, create `.planning/phases/02-knowledge-extraction-rag/02-03-SUMMARY.md`
</output>
