---
phase: 01-foundation-data-ingestion
plan: 05
type: execute
wave: 3
depends_on:
  - 01-04
files_modified:
  - src/pipeline/chunker/__init__.py
  - src/pipeline/chunker/text_chunker.py
  - src/pipeline/chunker/telegram_chunker.py
  - src/pipeline/indexer/__init__.py
  - src/pipeline/indexer/embedder.py
  - src/pipeline/indexer/qdrant_store.py
  - tests/pipeline/test_chunker.py
  - tests/pipeline/test_qdrant_indexer.py
autonomous: true
requirements:
  - ING-06

must_haves:
  truths:
    - "Текст разбивается на чанки по 300-500 токенов с перекрытием 1 предложение (D-12)"
    - "Markdown-структура сохраняется при чанкинге -- разбиение учитывает заголовки и параграфы (D-14)"
    - "Telegram сообщения группируются хронологически (окно 30 мин) перед чанкированием (D-15)"
    - "Каждый чанк содержит метаданные: дата, автор, источник, тип (D-13)"
    - "Dense + sparse эмбеддинги генерируются через FastEmbed"
    - "Qdrant коллекция создаётся с dense (e5-large, 1024d) + sparse (BM42) векторами"
    - "Чанки с эмбеддингами и метаданными upsert в Qdrant"
  artifacts:
    - path: "src/pipeline/chunker/text_chunker.py"
      provides: "Sentence-based chunking with overlap"
      exports: ["chunk_text"]
      contains: "def chunk_text"
    - path: "src/pipeline/chunker/telegram_chunker.py"
      provides: "Telegram-specific chrono-grouped chunking"
      exports: ["chunk_telegram_messages"]
      contains: "def chunk_telegram_messages"
    - path: "src/pipeline/indexer/embedder.py"
      provides: "Dense + sparse embedding generation"
      exports: ["Embedder"]
      contains: "class Embedder"
    - path: "src/pipeline/indexer/qdrant_store.py"
      provides: "Qdrant collection management and upsert"
      exports: ["QdrantStore"]
      contains: "class QdrantStore"
    - path: "tests/pipeline/test_chunker.py"
      provides: "Chunker tests"
      contains: "def test_chunk"
    - path: "tests/pipeline/test_qdrant_indexer.py"
      provides: "Qdrant indexer tests"
      contains: "def test_upsert"
  key_links:
    - from: "src/pipeline/chunker/telegram_chunker.py"
      to: "src/pipeline/filters/grouping.py"
      via: "import group_messages_chronologically"
      pattern: "group_messages_chronologically"
    - from: "src/pipeline/chunker/telegram_chunker.py"
      to: "src/pipeline/chunker/text_chunker.py"
      via: "import chunk_text"
      pattern: "chunk_text"
    - from: "src/pipeline/indexer/qdrant_store.py"
      to: "src/pipeline/indexer/embedder.py"
      via: "Embedder for vector generation"
      pattern: "Embedder"
    - from: "src/pipeline/indexer/qdrant_store.py"
      to: "qdrant-client"
      via: "QdrantClient"
      pattern: "QdrantClient"
---

<objective>
Построить чанкинг (text + Telegram), генерацию эмбеддингов (dense + sparse) и индексацию в Qdrant. Это заключительный этап pipeline-обработки данных.

Purpose: Чанкинг и индексация -- финальные шаги обработки перед тем как данные становятся доступны для RAG. Без этого знания не доступны для поиска.
Output: Text chunker, Telegram chunker, Embedder, QdrantStore, и их тесты.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-foundation-data-ingestion/01-PATTERNS.md
@.planning/phases/01-foundation-data-ingestion/01-RESEARCH.md
@.planning/phases/01-foundation-data-ingestion/01-CONTEXT.md

<interfaces>
<!-- Contracts from prior plans: -->

From Plan 01 -- src/core/config.py:
```python
class Settings(BaseSettings):
    dense_model_name: str = "intfloat/multilingual-e5-large"
    sparse_model_name: str = "Qdrant/bm42-all-MiniLM-L6-v2-quantized"
    qdrant_host: str = "localhost"
    qdrant_port: int = 6333
settings = Settings()
```

From Plan 02 -- TelegramMessage + grouping:
```python
# src/pipeline/parsers/telegram.py
@dataclass
class TelegramMessage:
    id: int; date: str; author: str; author_id: str; text: str
    is_service: bool; is_bot: bool; media_type: Optional[str]
    voice_path: Optional[str]; edit_date: Optional[str] = None

# src/pipeline/filters/grouping.py
def group_messages_chronologically(messages: list, window_minutes: int = 30) -> list[list]
```

From Plan 01 -- tests/conftest.py:
```python
@pytest.fixture
def mock_qdrant_client():
    client = MagicMock()
    client.collection_exists.return_value = True
    return client
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build text chunker and Telegram chunker with tests</name>
  <files>src/pipeline/chunker/__init__.py, src/pipeline/chunker/text_chunker.py, src/pipeline/chunker/telegram_chunker.py, tests/pipeline/test_chunker.py</files>
  <read_first>
    @.planning/phases/01-foundation-data-ingestion/01-PATTERNS.md (Pattern Assignments for text_chunker.py and telegram_chunker.py -- EXACT code)
    @.planning/phases/01-foundation-data-ingestion/01-CONTEXT.md (D-12, D-13, D-14, D-15 decisions)
  </read_first>
  <behavior>
    Text chunker tests:
    - Test 1: Short text (< 300 tokens) returns single chunk
    - Test 2: Long text produces multiple chunks each within 300-500 token range (approximate via char count)
    - Test 3: Chunks have 1 sentence overlap (last sentence of chunk N is first of chunk N+1)
    - Test 4: Markdown headings respected -- no chunk splits across heading boundary
    - Test 5: Empty text returns empty list

    Telegram chunker tests:
    - Test 6: Messages grouped chronologically then chunked
    - Test 7: Each chunk has metadata: source_type="telegram", date_range, authors list, chunk_index
    - Test 8: Author prefixes included: "[AuthorName]: message text"
    - Test 9: Single message group produces chunks with correct metadata
  </behavior>
  <action>
Создать src/pipeline/chunker/__init__.py (пустой).

RED: Создать tests/pipeline/test_chunker.py:

```python
# === Text Chunker Tests ===
def test_short_text_single_chunk:
    text = "Короткий текст."
    chunks = chunk_text(text)
    assert len(chunks) == 1
    assert chunks[0] == "Короткий текст."

def test_long_text_multiple_chunks:
    # Текст ~600 токенов (2100+ символов) -- должен разбиться на 2+ чанка
    text = "Предложение раз. " * 100  # ~2000 символов
    chunks = chunk_text(text)
    assert len(chunks) >= 2

def test_overlap_between_chunks:
    text = "Первое предложение. Второе. Третье. Четвёртое. Пятое. Шестое. Седьмое. Восьмое. Девятое. Десятое. " * 5
    chunks = chunk_text(text, overlap_sentences=1)
    # Last sentence of chunk[i] should appear in chunk[i+1]
    if len(chunks) > 1:
        assert any(sentence overlap)

def test_markdown_heading_respected:
    text = "## Заголовок 1\nПараграф текста.\n\n## Заголовок 2\nДругой параграф."
    chunks = chunk_text(text)
    # Ни один чанк не должен начинаться с продолжения после заголовка

def test_empty_text_returns_empty:
    assert chunk_text("") == []

# === Telegram Chunker Tests ===
def test_telegram_chunker_groups_and_chunks:
    messages = [TelegramMessage(id=1, date="2024-01-15T10:00:00", ...), ...]
    chunks = chunk_telegram_messages(messages, window_minutes=30)
    assert all("source_type" in c["metadata"] for c in chunks)
    assert all(c["metadata"]["source_type"] == "telegram" for c in chunks)

def test_metadata_present:
    # Проверить date_range, authors, chunk_index в metadata

def test_author_prefixes:
    # Проверить что текст чанка содержит "[AuthorName]: message"
```

GREEN: Создать src/pipeline/chunker/text_chunker.py по PATTERNS.md:
- TARGET_TOKENS = 400, MAX_TOKENS = 500, MIN_TOKENS = 300
- CHARS_PER_TOKEN = 3.5 (приближение для русского)
- chunk_text(text: str, overlap_sentences: int = 1) -> list[str]
- _split_markdown_paragraphs(text: str) -> list[str] -- split на heading boundaries (D-14)
- _split_into_sentences(paragraphs: list[str]) -> list[str] -- regex split на [.!?] (НЕ nltk -- Pitfall)

Создать src/pipeline/chunker/telegram_chunker.py по PATTERNS.md:
- chunk_telegram_messages(messages: list, window_minutes: int = 30) -> list[dict]
- Группирует через group_messages_chronologically (D-15)
- Конкатенирует текст группы с "[author]: text" форматом
- Chunk каждую группу через chunk_text
- Добавляет metadata: source_type, date_range, authors, chat, chunk_index (D-13)

Запустить: `uv run pytest tests/pipeline/test_chunker.py -x -v`
  </action>
  <verify>
    <automated>cd /Users/iliazarikov/Documents/Python\ Projects/onboarding_bot && uv run pytest tests/pipeline/test_chunker.py -x -v</automated>
  </verify>
  <done>
    - text_chunker разбивает на чанки 300-500 токенов с перекрытием 1 предложение (D-12)
    - Markdown-aware: заголовки не разрываются (D-14)
    - telegram_chunker группирует хронологически + чанкирует (D-15)
    - Каждый чанк имеет metadata: date_range, authors, source_type, chunk_index (D-13)
    - Все тесты проходят
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Build Embedder and QdrantStore with tests</name>
  <files>src/pipeline/indexer/__init__.py, src/pipeline/indexer/embedder.py, src/pipeline/indexer/qdrant_store.py, tests/pipeline/test_qdrant_indexer.py</files>
  <read_first>
    @.planning/phases/01-foundation-data-ingestion/01-PATTERNS.md (Pattern Assignments for embedder.py and qdrant_store.py -- EXACT code)
    @.planning/phases/01-foundation-data-ingestion/01-RESEARCH.md (Pattern 3: Qdrant Hybrid Search, Pitfall 1: BM42 vs SPLADE, Pitfall 5: ID conflicts)
  </read_first>
  <behavior>
    Embedder tests (mock FastEmbed):
    - Test 1: embed_batch returns tuple of (dense_vectors, sparse_vectors)
    - Test 2: Dense vectors have correct dimensionality (1024 for e5-large)
    - Test 3: Sparse vectors have indices and values attributes

    QdrantStore tests (mock QdrantClient):
    - Test 4: ensure_collection creates collection with dense (1024, COSINE) + sparse config
    - Test 5: upsert_chunks calls client.upsert with correct PointStruct format
    - Test 6: Point IDs use composite format source_type:source_id:chunk_index (Pitfall 5)
    - Test 7: delete_by_source calls client.delete with correct filter
    - Test 8: Payload includes text and metadata fields
  </behavior>
  <action>
Создать src/pipeline/indexer/__init__.py (пустой).

RED: Создать tests/pipeline/test_qdrant_indexer.py:

```python
# === Embedder Tests ===
def test_embed_batch_returns_tuple:
    embedder = Embedder()
    dense, sparse = embedder.embed_batch(["текст 1", "текст 2"])
    assert len(dense) == 2
    assert len(sparse) == 2

def test_dense_vector_dimensionality:
    embedder = Embedder()
    dense, _ = embedder.embed_batch(["текст"])
    assert dense[0].shape[0] == 1024  # e5-large

# === QdrantStore Tests (mocked) ===
def test_ensure_collection_creates_with_config(mock_qdrant_client):
    store = QdrantStore(mock_qdrant_client)
    mock_qdrant_client.collection_exists.return_value = False
    store.ensure_collection()
    mock_qdrant_client.create_collection.assert_called_once()
    call_kwargs = mock_qdrant_client.create_collection.call_args[1]
    assert call_kwargs["vectors_config"]["dense"].size == 1024
    assert "sparse" in call_kwargs["sparse_vectors_config"]

def test_upsert_chunks_correct_format(mock_qdrant_client):
    store = QdrantStore(mock_qdrant_client)
    chunks = [{
        "id": "telegram:src1:0",
        "text": "текст",
        "dense_vector": [0.1] * 1024,
        "sparse_vector": MagicMock(indices=[1,2,3], values=[0.5,0.3,0.2]),
        "metadata": {"source_type": "telegram", "date_range": "..."},
    }]
    count = store.upsert_chunks(chunks)
    assert count == 1
    mock_qdrant_client.upsert.assert_called_once()

def test_composite_ids:
    # Pitfall 5: verify composite ID format
    store = QdrantStore(mock_qdrant_client)
    chunks = [{"id": "telegram:source123:0", ...}]
    store.upsert_chunks(chunks)
    points = mock_qdrant_client.upsert.call_args[1]["points"]
    assert points[0].id == "telegram:source123:0"

def test_delete_by_source(mock_qdrant_client):
    store = QdrantStore(mock_qdrant_client)
    store.delete_by_source("source123")
    mock_qdrant_client.delete.assert_called_once()

def test_payload_includes_text_and_metadata(mock_qdrant_client):
    store = QdrantStore(mock_qdrant_client)
    chunks = [{"id": "t:1:0", "text": "текст", "dense_vector": [0.1]*1024,
               "sparse_vector": MagicMock(indices=[], values=[]),
               "metadata": {"source_type": "pdf"}}]
    store.upsert_chunks(chunks)
    points = mock_qdrant_client.upsert.call_args[1]["points"]
    assert points[0].payload["text"] == "текст"
    assert points[0].payload["source_type"] == "pdf"
```

GREEN: Создать src/pipeline/indexer/embedder.py по PATTERNS.md:
- class Embedder:
  - __init__(dense_model="intfloat/multilingual-e5-large", sparse_model="Qdrant/bm42-all-MiniLM-L6-v2-quantized")
  - _dense = TextEmbedding(model_name=dense_model)
  - _sparse = SparseTextEmbedding(model_name=sparse_model)
  - embed_batch(texts: list[str]) -> tuple[list, list]: dense vectors + sparse vectors
  - batch_size=32 для dense

Создать src/pipeline/indexer/qdrant_store.py по PATTERNS.md:
- class QdrantStore:
  - COLLECTION_NAME = "knowledge"
  - __init__(client: QdrantClient)
  - ensure_collection(dense_size=1024): создаёт коллекцию с dense (COSINE, HNSW m=16 ef_construct=100) + sparse (SparseVectorParams)
  - upsert_chunks(chunks: list[dict]) -> int: upsert PointStruct с vector={dense, sparse} и payload
  - delete_by_source(source_id: str): delete с filter по source_id (Pitfall 5)

Запустить: `uv run pytest tests/pipeline/test_qdrant_indexer.py -x -v`
  </action>
  <verify>
    <automated>cd /Users/iliazarikov/Documents/Python\ Projects/onboarding_bot && uv run pytest tests/pipeline/test_qdrant_indexer.py -x -v</automated>
  </verify>
  <done>
    - Embedder генерирует dense (1024d) + sparse (BM42) эмбеддинги
    - QdrantStore создаёт коллекцию с hybrid config (dense + sparse)
    - Composite IDs: source_type:source_id:chunk_index (Pitfall 5)
    - Metadata в payload: text, source_type, date_range, authors, chunk_index
    - delete_by_source очищает записи при повторном ингесте
    - Все тесты проходят
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Application -> Qdrant | Vector DB on localhost (Docker network) |
| FastEmbed model loading | Local model download from HuggingFace hub |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-14 | Denial of Service | Embedding generation | accept | CPU-intensive operation but runs in Celery worker (async). No user-triggered synchronous path |
| T-01-15 | Tampering | Qdrant data integrity | accept | Single-tenant system, no multi-user access to Qdrant. Re-ingest handles conflicts via delete+upsert |
| T-01-16 | Information Disclosure | Qdrant payload | mitigate | Payload contains anonymized text only (PII removed by Plan 04). Metadata is non-sensitive |
</threat_model>

<verification>
- `uv run pytest tests/pipeline/test_chunker.py -x -v` passes
- `uv run pytest tests/pipeline/test_qdrant_indexer.py -x -v` passes
- Text chunker respects D-12 (300-500 tokens) and D-14 (Markdown-aware)
- Telegram chunker groups chronologically per D-15
- Embedder produces 1024-dim dense vectors
- QdrantStore creates hybrid collection (dense + sparse)
</verification>

<success_criteria>
- Чанкинг: 5-10 предложений, 300-500 токенов, перекрытие 1 предложение (D-12)
- Markdown-aware разбиение (D-14)
- Telegram: хронологическая группировка перед чанкированием (D-15)
- Metadata: дата, автор, источник, тип на каждом чанке (D-13)
- Dense (e5-large, 1024d) + sparse (BM42) эмбеддинги через FastEmbed
- Qdrant коллекция с hybrid search (dense + sparse)
- Composite IDs для safe re-ingest (Pitfall 5)
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-data-ingestion/01-05-SUMMARY.md`
</output>
