---
phase: 01-foundation-data-ingestion
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/pipeline/__init__.py
  - src/pipeline/parsers/__init__.py
  - src/pipeline/parsers/telegram.py
  - src/pipeline/filters/__init__.py
  - src/pipeline/filters/noise.py
  - src/pipeline/filters/grouping.py
  - tests/pipeline/__init__.py
  - tests/pipeline/test_telegram_parser.py
  - tests/pipeline/test_noise_filter.py
autonomous: true
requirements:
  - ING-02
  - ING-04

must_haves:
  truths:
    - "Telegram JSON export (result.json) парсится и извлекает текстовые сообщения с метаданными (id, date, author, author_id)"
    - "Rich-text формат Telegram (массив объектов) корректно конвертируется в строку"
    - "Service messages (type=service) отфильтровываются при парсинге"
    - "Голосовые сообщения распознаются и связываются с .ogg файлами"
    - "Шумовые сообщения (bot messages, пустые, "+", "ок") отфильтровываются noise filter"
    - "Короткие содержательные сообщения НЕ отфильтровываются (D-10)"
  artifacts:
    - path: "src/pipeline/parsers/telegram.py"
      provides: "Telegram JSON export parser"
      exports: ["TelegramMessage", "parse_text_field", "parse_telegram_export"]
      contains: "class TelegramMessage"
    - path: "src/pipeline/filters/noise.py"
      provides: "Telegram noise filter"
      exports: ["is_noise", "filter_messages"]
      contains: "def is_noise"
    - path: "src/pipeline/filters/grouping.py"
      provides: "Chronological message grouping"
      exports: ["group_messages_chronologically"]
      contains: "def group_messages_chronologically"
    - path: "tests/pipeline/test_telegram_parser.py"
      provides: "Parser unit tests"
      contains: "def test_parse"
    - path: "tests/pipeline/test_noise_filter.py"
      provides: "Noise filter unit tests"
      contains: "def test_noise"
  key_links:
    - from: "src/pipeline/parsers/telegram.py"
      to: "beautifulsoup4"
      via: "HTML cleanup"
      pattern: "BeautifulSoup"
    - from: "src/pipeline/filters/noise.py"
      to: "src/pipeline/parsers/telegram.py"
      via: "TelegramMessage dataclass"
      pattern: "TelegramMessage"
    - from: "tests/pipeline/test_telegram_parser.py"
      to: "tests/conftest.py"
      via: "sample_telegram_json fixture"
      pattern: "sample_telegram_json"
---

<objective>
Построить Telegram парсер (result.json), шумовой фильтр и хронологическую группировку. Это независимый от инфраструктуры pipeline-компонент, который можно тестировать без Docker сервисов.

Purpose: Покрывает ING-02 (парсинг Telegram JSON) и ING-04 (фильтрация шума). Парсер -- первый этап пайплайна ингеста.
Output: Telegram парсер, шумовой фильтр, grouping, и их тесты.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-foundation-data-ingestion/01-PATTERNS.md
@.planning/phases/01-foundation-data-ingestion/01-RESEARCH.md
@.planning/phases/01-foundation-data-ingestion/01-CONTEXT.md

<interfaces>
<!-- Contracts from Plan 01 that this plan depends on: -->

From Plan 01 -- tests/conftest.py (fixtures this plan uses):
```python
@pytest.fixture
def sample_telegram_messages():
    return [
        {"id": 1, "type": "message", "date": "2024-01-15T10:00:00",
         "from": "Иван Иванов", "from_id": "user123",
         "text": "Позвони Александру по номеру +7(903)123-45-67"},
        {"id": 2, "type": "service", "date": "2024-01-15T10:01:00",
         "from": "Иван Иванов", "from_id": "user123",
         "text": "Иван Иванов joined the group"},
        {"id": 3, "type": "message", "date": "2024-01-15T10:05:00",
         "from": "SomeBot", "from_id": "bot456",
         "text": "Reminder: meeting at 15:00"},
    ]

@pytest.fixture
def sample_telegram_json(tmp_path, sample_telegram_messages):
    data = {"name": "Test Chat", "messages": sample_telegram_messages}
    path = tmp_path / "result.json"
    path.write_text(json.dumps(data, ensure_ascii=False), encoding="utf-8")
    return str(path)
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build Telegram JSON parser with tests</name>
  <files>src/pipeline/__init__.py, src/pipeline/parsers/__init__.py, src/pipeline/parsers/telegram.py, tests/pipeline/__init__.py, tests/pipeline/test_telegram_parser.py</files>
  <read_first>
    @.planning/phases/01-foundation-data-ingestion/01-PATTERNS.md (Pattern Assignment for src/pipeline/parsers/telegram.py -- EXACT code)
    @.planning/phases/01-foundation-data-ingestion/01-RESEARCH.md (Pitfall 2: text field string vs array)
  </read_first>
  <behavior>
    - Test 1: parse_telegram_export returns TelegramMessage objects from valid JSON with metadata (id, date, author, author_id)
    - Test 2: Service messages (type="service") are excluded from results per D-09
    - Test 3: Rich-text array format [{"type": "bold", "text": "hello"}] is correctly concatenated into plain string (Pitfall 2)
    - Test 4: Voice messages with media_type="voice_message" have voice_path populated (D-17)
    - Test 5: HTML in text field is stripped via BeautifulSoup
    - Test 6: Empty messages without text and not voice are skipped
    - Test 7: Messages with edit_date field preserve latest version
  </behavior>
  <action>
Создать src/pipeline/__init__.py (пустой) и src/pipeline/parsers/__init__.py (пустой).
Создать tests/pipeline/__init__.py (пустой).

RED: Создать tests/pipeline/test_telegram_parser.py с тестами:
- test_parse_regular_messages: создать JSON с обычными сообщениями, проверить что возвращаются TelegramMessage с правильными полями
- test_service_messages_excluded: JSON содержит service message (type="service") -- результат не содержит его (D-09)
- test_rich_text_array: сообщение с text=[{"type": "plain", "text": "Hello "}, {"type": "bold", "text": "world"}] -- результат содержит "Hello world"
- test_voice_message_linked: сообщение с media_type="voice_message" и file_name="voice.ogg" -- TelegramMessage.voice_path == "voice.ogg" (D-17)
- test_html_stripped: сообщение с text="<b>bold</b> text" -- результат содержит "bold text" без HTML тегов
- test_empty_text_non_voice_skipped: сообщение без text и не voice -- пропущено
- test_edited_message: сообщение с edit_date -- TelegramMessage.edit_date populated

GREEN: Создать src/pipeline/parsers/telegram.py точно по PATTERNS.md Pattern Assignment:
- TelegramMessage dataclass: id (int), date (str), author (str), author_id (str), text (str), is_service (bool), is_bot (bool), media_type (Optional[str]), voice_path (Optional[str]), edit_date (Optional[str])
- parse_text_field(text_field) -- handles both str и list (Pitfall 2)
- parse_telegram_export(filepath: str) -> list[TelegramMessage]:
  - Skip type=="service" (D-09)
  - Parse text via parse_text_field
  - Skip empty non-voice messages
  - BeautifulSoup для HTML cleanup
  - Detect bots via "bot" in from_id.lower()
  - Link voice messages via file_name (D-17)
  - Store edit_date (Pitfall -- edited messages)

Запустить: `uv run pytest tests/pipeline/test_telegram_parser.py -x -v`
  </action>
  <verify>
    <automated>cd /Users/iliazarikov/Documents/Python\ Projects/onboarding_bot && uv run pytest tests/pipeline/test_telegram_parser.py -x -v</automated>
  </verify>
  <done>
    - Все 7 тестов проходят
    - parse_telegram_export корректно обрабатывает rich-text array (Pitfall 2)
    - Service messages исключены (D-09)
    - Voice messages связаны с .ogg файлами (D-17)
    - HTML теги удалены из text
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Build noise filter and chronological grouping with tests</name>
  <files>src/pipeline/filters/__init__.py, src/pipeline/filters/noise.py, src/pipeline/filters/grouping.py, tests/pipeline/test_noise_filter.py</files>
  <read_first>
    @.planning/phases/01-foundation-data-ingestion/01-PATTERNS.md (Pattern Assignments for noise.py and grouping.py -- EXACT code)
    @.planning/phases/01-foundation-data-ingestion/01-CONTEXT.md (D-09, D-10, D-11 decisions)
  </read_first>
  <behavior>
    Noise filter tests:
    - Test 1: Service messages (is_service=True) are noise
    - Test 2: Bot messages (is_bot=True) are noise
    - Test 3: Exact noise words ("ок", "спасибо", "+") are noise
    - Test 4: Short valuable messages ("звони Ивану", "проверь накладную") are NOT noise (D-10)
    - Test 5: Empty/whitespace-only messages are noise
    - Test 6: Normal messages pass through filter_messages

    Grouping tests:
    - Test 7: Messages within 30 minutes grouped together
    - Test 8: Messages with >30 minute gap start new group
    - Test 9: Empty list returns empty list
    - Test 10: Single message returns single group
  </behavior>
  <action>
Создать src/pipeline/filters/__init__.py (пустой).

RED: Создать tests/pipeline/test_noise_filter.py:
- test_service_is_noise: is_noise с is_service=True возвращает True
- test_bot_is_noise: is_noise с is_bot=True возвращает True
- test_exact_noise_words: is_noise("ок", ...) == True; is_noise("спасибо", ...) == True; is_noise("+", ...) == True
- test_short_valuable_not_noise: is_noise("звони Ивану", is_service=False, is_bot=False, author="user") == False (D-10)
- test_empty_is_noise: is_noise("", is_service=False, is_bot=False, author="user") == True
- test_filter_messages_removes_noise: filter_messages(messages) возвращает только non-noise сообщения
- test_grouping_within_window: 3 сообщения в пределах 30 мин -- 1 группа
- test_grouping_across_gap: 2 сообщения с gap > 30 мин -- 2 группы
- test_grouping_empty: group_messages_chronologically([]) == []
- test_grouping_single: group_messages_chronologically([msg]) == [[msg]]

GREEN: Создать src/pipeline/filters/noise.py по PATTERNS.md:
- NOISE_EXACT set: {"ok", "оки", "ок!", "ок.", "спасибо", "thanks", "thx", "да", "привет", "+"}
- NOISE_PATTERNS: empty string, dots/dashes only, single +/-
- is_noise(message_text: str, is_service: bool, is_bot: bool, author: str) -> bool
  - D-09: фильтрует service, bot, шумовые слова
  - D-10: НЕ фильтрует по длине -- короткие сообщения с содержанием проходят
- filter_messages(messages: list[TelegramMessage]) -> list[TelegramMessage]

Создать src/pipeline/filters/grouping.py по PATTERNS.md:
- group_messages_chronologically(messages: list, window_minutes: int = 30) -> list[list]
- _parse_date(date_str: str) -> datetime
- D-15: окно по умолчанию 30 минут (configurable)

Запустить: `uv run pytest tests/pipeline/test_noise_filter.py -x -v`
  </action>
  <verify>
    <automated>cd /Users/iliazarikov/Documents/Python\ Projects/onboarding_bot && uv run pytest tests/pipeline/test_noise_filter.py -x -v</automated>
  </verify>
  <done>
    - Все 10 тестов проходят
    - Noise filter реализует D-09 (что фильтруем) и D-10 (что НЕ фильтруем)
    - Chronological grouping реализует D-15 (окно 30 мин)
    - Короткие сообщения типа "звони Ивану" проходят фильтр (D-10)
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Telegram JSON file -> Parser | Untrusted user-uploaded file content |
| Parser -> Noise filter | Internal pipeline data flow |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-06 | Tampering | Telegram JSON parser | mitigate | Validate JSON structure, handle malformed data gracefully with try/except, limit message count to prevent memory exhaustion |
| T-01-07 | Information Disclosure | Telegram parser output | mitigate | Parser preserves raw PII in output -- but PII anonymization (Plan 04) happens next in pipeline per D-07 |
</threat_model>

<verification>
- `uv run pytest tests/pipeline/test_telegram_parser.py -x -v` passes
- `uv run pytest tests/pipeline/test_noise_filter.py -x -v` passes
- TelegramMessage dataclass has all required fields (id, date, author, author_id, text, is_service, is_bot, media_type, voice_path, edit_date)
- parse_text_field handles both str and list inputs
- is_noise correctly filters per D-09 but preserves short messages per D-10
</verification>

<success_criteria>
- Telegram JSON parser корректно обрабатывает все форматы text (string и array)
- Service messages и bot messages отфильтрованы
- Voice messages связаны с .ogg файлами
- Noise filter отсекает шум, но сохраняет короткие содержательные сообщения
- Chronological grouping группирует по окну 30 минут
- Все тесты проходят
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-data-ingestion/01-02-SUMMARY.md`
</output>
