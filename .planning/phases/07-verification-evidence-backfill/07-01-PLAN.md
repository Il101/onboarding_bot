---
phase: 07-verification-evidence-backfill
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/verify_backfill.py
  - tests/planning/test_verification_backfill_rules.py
  - .planning/functional-gaps.md
autonomous: true
requirements:
  - ING-01
  - ING-02
  - ING-03
  - ING-04
  - ING-05
  - ING-06
  - KNW-01
  - KNW-02
  - KNW-03
  - KNW-04
  - BOT-01
  - BOT-02
  - BOT-03
  - BOT-04
must_haves:
  truths:
    - "Детерминированный verifier для strict-schema, 3-source completeness и anchor-resolvability существует и тестами подтвержден."
    - "Functional gaps в рамках Phase 7 не чинятся кодом, а маршрутизируются в register для отдельных gap phases."
    - "Статус-классификация satisfied/partial/unsatisfied/orphaned воспроизводима CLI-проверкой."
  artifacts:
    - path: "scripts/verify_backfill.py"
      provides: "Deterministic verification evidence validator with strict gate assertions"
    - path: "tests/planning/test_verification_backfill_rules.py"
      provides: "Regression protection for parser, evidence completeness, and CLI assertions"
    - path: ".planning/functional-gaps.md"
      provides: "Canonical register for functional gaps discovered during evidence backfill"
  key_links:
    - from: "scripts/verify_backfill.py"
      to: ".planning/phases/0*-*/0*-VERIFICATION.md"
      via: "strict schema + 3-source + anchor assertions"
      pattern: "--strict-schema --assert-resolvable-anchors"
    - from: "tests/planning/test_verification_backfill_rules.py"
      to: "scripts/verify_backfill.py"
      via: "CLI contract and status classification checks"
      pattern: "satisfied | partial | unsatisfied | orphaned"
    - from: "scripts/verify_backfill.py"
      to: ".planning/functional-gaps.md"
      via: "functional-gap routing output"
      pattern: "requirement_id, phase, evidence, severity, target_gap_phase"
---

<objective>
Build deterministic verification backfill tooling and gap register schema used by all subsequent Phase 7 plans.
</objective>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Implement strict 3-source validator and anchor checks</name>
  <files>scripts/verify_backfill.py, tests/planning/test_verification_backfill_rules.py, .planning/functional-gaps.md</files>
  <read_first>
    - .planning/phases/07-verification-evidence-backfill/07-CONTEXT.md
    - .planning/phases/07-verification-evidence-backfill/07-RESEARCH.md
    - .planning/v1-MILESTONE-AUDIT.md
  </read_first>
  <action>Create `scripts/verify_backfill.py` with strict schema checks (frontmatter + requirements table), 3-source evidence completeness, `--assert-resolvable-anchors`, and `--assert-no-orphaned-phases`. Add tests in `tests/planning/test_verification_backfill_rules.py`. Initialize `.planning/functional-gaps.md` format for out-of-scope functional gaps.</action>
  <acceptance_criteria>
    - `scripts/verify_backfill.py` supports: `--phase`, `--changed-only`, `--strict-schema`, `--assert-resolvable-anchors`, `--assert-no-orphaned-phases`.
    - `tests/planning/test_verification_backfill_rules.py` exists and passes.
    - `.planning/functional-gaps.md` exists with columns: requirement_id, phase, evidence, severity, target_gap_phase.
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest tests/planning/test_verification_backfill_rules.py -q</automated>
  </verify>
  <done>Deterministic verifier is ready for phase-level artifact backfill.</done>
</task>
</tasks>

<verification>
- `uv run pytest tests/planning/test_verification_backfill_rules.py -q`
</verification>

<output>
After completion, create `.planning/phases/07-verification-evidence-backfill/07-01-SUMMARY.md`
</output>
