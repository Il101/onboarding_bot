---
phase: 04-runtime-integration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/tasks/ingest.py
  - src/tasks/knowledge.py
  - tests/phase4/test_ingest_orchestration.py
  - tests/phase4/test_runtime_integration_flow.py
autonomous: true
requirements:
  - KNW-01
  - KNW-02
  - BOT-01
must_haves:
  truths:
    - "После успешного ingest автоматически запускается extraction+SOP pipeline без ручного шага."
    - "Опубликованные knowledge units формируются из extraction flow и доступны для последующего retrieval."
    - "Сквозной runtime path от ingest к extraction не ломает существующий ingest API контракт."
  artifacts:
    - path: "src/tasks/ingest.py"
      provides: "Runtime orchestration trigger from ingest to knowledge tasks"
      contains: "extract_knowledge_task"
    - path: "src/tasks/knowledge.py"
      provides: "Extraction + SOP task interfaces used by ingest orchestration"
      exports: ["extract_knowledge_task", "generate_sop_task"]
    - path: "tests/phase4/test_ingest_orchestration.py"
      provides: "Proof that ingest dispatches extraction/SOP path"
    - path: "tests/phase4/test_runtime_integration_flow.py"
      provides: "End-to-end runtime wiring assertions for Phase 4 wave 1"
  key_links:
    - from: "src/tasks/ingest.py"
      to: "src/tasks/knowledge.py"
      via: "celery task dispatch with source_id and chunk payload"
      pattern: "extract_knowledge_task"
    - from: "src/tasks/knowledge.py"
      to: "src/pipeline/indexer/knowledge_writer.py"
      via: "publishable units indexing into knowledge collection"
      pattern: "index_knowledge_units"
---

<objective>
Implement runtime orchestration so ingestion automatically continues into extraction and SOP generation.

Purpose: close KNW-01/KNW-02 integration gap and establish real data flow used by bot retrieval in Phase 4.
Output: updated ingest task orchestration and phase4 integration tests for dispatch and end-to-end wiring.
</objective>

<execution_context>
@.github/get-shit-done/workflows/execute-plan.md
@.github/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/v1-MILESTONE-AUDIT.md
@.planning/phases/04-runtime-integration/04-RESEARCH.md
@.planning/phases/04-runtime-integration/04-PATTERNS.md
@.planning/phases/02-knowledge-extraction-rag/02-02-SUMMARY.md
@src/tasks/ingest.py
@src/tasks/knowledge.py
@src/pipeline/indexer/knowledge_writer.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wire ingest completion to extraction and SOP dispatch</name>
  <files>src/tasks/ingest.py, src/tasks/knowledge.py, tests/phase4/test_ingest_orchestration.py</files>
  <behavior>
    - Test 1: successful telegram ingest dispatches extraction task with source_id and built chunks.
    - Test 2: extraction completion path dispatches SOP generation with grouped publishable units.
    - Test 3: orchestration failures surface controlled task failure state and do not silently pass.
  </behavior>
  <action>Extend ingest runtime flow to trigger `extract_knowledge_task` and `generate_sop_task` after chunk indexing, preserving existing ingest return envelope and progress stages. Reuse existing Celery task signatures; do not introduce new queue framework. Keep dispatch arguments deterministic and serializable.</action>
  <verify>
    <automated>uv run pytest tests/phase4/test_ingest_orchestration.py -q</automated>
  </verify>
  <done>Ingestion runtime deterministically dispatches extraction/SOP pipeline and tests prove call-chain integrity.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add runtime flow coverage for ingestion to publishable knowledge path</name>
  <files>tests/phase4/test_runtime_integration_flow.py, tests/phase2/test_extraction_pipeline.py</files>
  <behavior>
    - Test 1: publishable extraction output from dispatched flow reaches index writer path.
    - Test 2: low-confidence units remain review-only and are not published/indexed.
    - Test 3: runtime flow remains compatible with existing phase2 extraction invariants.
  </behavior>
  <action>Add focused integration tests that patch external dependencies and assert ingest -> extraction -> grouped publishable units flow behavior. Reuse phase2 extraction fixtures/contracts for atomic fact, confidence gate, and grouping semantics.</action>
  <verify>
    <automated>uv run pytest tests/phase4/test_runtime_integration_flow.py tests/phase2/test_extraction_pipeline.py -q</automated>
  </verify>
  <done>Phase 4 has automated proof that runtime orchestration produces publishable knowledge path without regressing extraction contracts.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ingest task payload -> extraction orchestration | Untrusted source content crosses into async pipeline dispatch |
| extraction output -> indexing | Generated units cross from model output to persistent vector store |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | T | `src/tasks/ingest.py` | mitigate | Validate dispatch inputs and preserve deterministic staged flow; reject malformed payload propagation. |
| T-04-02 | I | `src/tasks/knowledge.py` | mitigate | Keep confidence gate before publish/index and test low-confidence isolation path. |
| T-04-03 | D | `src/tasks/ingest.py` | mitigate | Use bounded retries and explicit failure states; avoid infinite orchestration retries. |
</threat_model>

<verification>
- `uv run pytest tests/phase4/test_ingest_orchestration.py -q`
- `uv run pytest tests/phase4/test_runtime_integration_flow.py -q`
</verification>

<success_criteria>
- Runtime ingestion automatically triggers extraction and SOP tasks.
- Publishable knowledge path is test-proven and confidence gate semantics are preserved.
- KNW-01 and KNW-02 integration blockers from milestone audit are closed at runtime level.
</success_criteria>

<output>
After completion, create `.planning/phases/04-runtime-integration/04-01-SUMMARY.md`
</output>
