---
phase: 04-runtime-integration
plan: 02
type: execute
wave: 2
depends_on:
  - 04-01
files_modified:
  - src/ai/rag/retriever.py
  - src/api/routes/knowledge.py
  - src/api/main.py
  - tests/phase4/test_real_retrieval_query.py
  - tests/phase4/test_bot_grounded_query_contract.py
  - tests/phase2/test_low_relevance_fallback.py
autonomous: true
requirements:
  - KNW-03
  - KNW-04
  - BOT-01
must_haves:
  truths:
    - "`/api/knowledge/query` использует реальный retrieval из knowledge-индекса, а не seed candidates."
    - "Ответ API сохраняет стабильный envelope для бота: answer/confidence/sources/fallback_used."
    - "Каждый ответ содержит source attribution для ручной верификации."
    - "Bot retrieval adapter получает grounded payload из реальной базы знаний."
  artifacts:
    - path: "src/api/routes/knowledge.py"
      provides: "Real retrieval-backed knowledge query route"
      contains: "@router.post(\"/query\")"
    - path: "src/ai/rag/retriever.py"
      provides: "Retriever implementation used by query route in runtime mode"
      exports: ["HybridRetriever"]
    - path: "tests/phase4/test_real_retrieval_query.py"
      provides: "Phase4 assertions that query path no longer relies on seed payload"
    - path: "tests/phase4/test_bot_grounded_query_contract.py"
      provides: "Bot-facing query contract compatibility checks"
  key_links:
    - from: "src/api/routes/knowledge.py"
      to: "src/ai/rag/retriever.py"
      via: "query route invokes real retrieval path"
      pattern: "HybridRetriever"
    - from: "src/api/routes/knowledge.py"
      to: "src/ai/rag/synthesizer.py"
      via: "retrieved candidates are synthesized with fallback policy"
      pattern: "synthesize_answer"
    - from: "src/ai/langgraph/nodes/retrieve_phase2.py"
      to: "src/api/routes/knowledge.py"
      via: "bot adapter consumes stable response envelope"
      pattern: "/api/knowledge/query"
---

<objective>
Replace seed-based query logic with real retrieval-backed knowledge query while preserving bot contract compatibility.

Purpose: close KNW-03/KNW-04/BOT-01 audit gaps and ensure employee bot answers are grounded in enterprise knowledge.
Output: retrieval-backed query route and contract/integration tests proving grounded payload behavior.
</objective>

<execution_context>
@.github/get-shit-done/workflows/execute-plan.md
@.github/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/v1-MILESTONE-AUDIT.md
@.planning/phases/04-runtime-integration/04-RESEARCH.md
@.planning/phases/04-runtime-integration/04-PATTERNS.md
@.planning/phases/02-knowledge-extraction-rag/02-03-SUMMARY.md
@.planning/phases/03-telegram-bot/03-03-SUMMARY.md
@src/api/routes/knowledge.py
@src/ai/rag/retriever.py
@src/ai/rag/synthesizer.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Replace seed candidate route path with real retrieval candidates</name>
  <files>src/api/routes/knowledge.py, src/ai/rag/retriever.py, tests/phase4/test_real_retrieval_query.py</files>
  <behavior>
    - Test 1: query route does not use static seed candidate list.
    - Test 2: query route calls retriever and passes returned candidates to synthesizer.
    - Test 3: low-relevance fallback remains active and returns nearest sources.
  </behavior>
  <action>Refactor `/api/knowledge/query` to build candidates from real retriever path (knowledge collection data) and remove static `seed_candidates` behavior. Preserve request schema constraints and response envelope keys exactly. Keep fallback policy delegated to existing synthesizer.</action>
  <verify>
    <automated>uv run pytest tests/phase4/test_real_retrieval_query.py tests/phase2/test_low_relevance_fallback.py -q</automated>
  </verify>
  <done>Knowledge query route is retrieval-backed and no longer depends on embedded static candidates.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Prove bot-facing grounded contract remains compatible after retrieval migration</name>
  <files>tests/phase4/test_bot_grounded_query_contract.py, tests/phase3/test_multiturn_context.py, src/api/main.py</files>
  <behavior>
    - Test 1: route still returns exact envelope consumed by Phase 3 bot adapter.
    - Test 2: payload sources contain required attribution fields (`source_id`, `excerpt`, locator).
    - Test 3: bot retrieval adapter can consume migrated route output without adapter changes.
  </behavior>
  <action>Add compatibility tests between query route response and Phase 3 retrieval adapter expectations; reuse existing bot integration test style with mocked transport where needed. Keep API router registration intact in `src/api/main.py` and avoid introducing parallel endpoint versions.</action>
  <verify>
    <automated>uv run pytest tests/phase4/test_bot_grounded_query_contract.py tests/phase3/test_multiturn_context.py -q</automated>
  </verify>
  <done>Grounded query contract is preserved and bot integration remains stable after retrieval migration.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user query -> retrieval backend | Untrusted input enters retrieval and synthesis path |
| retrieval metadata -> API response | Internal source payload is exposed to external bot consumers |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-04 | I | `src/api/routes/knowledge.py` | mitigate | Enforce response attribution fields and test contract for source locator presence. |
| T-04-05 | T | `src/ai/rag/retriever.py` | mitigate | Remove static seed path and route only through retrieval-backed candidate assembly. |
| T-04-06 | D | `src/api/routes/knowledge.py` | mitigate | Keep bounded `top_k` validation and failure-safe envelope on route errors. |
</threat_model>

<verification>
- `uv run pytest tests/phase4/test_real_retrieval_query.py -q`
- `uv run pytest tests/phase4/test_bot_grounded_query_contract.py -q`
- `uv run pytest tests/phase2/test_low_relevance_fallback.py tests/phase3/test_multiturn_context.py -q`
</verification>

<success_criteria>
- `/api/knowledge/query` is retrieval-backed and no longer seed-backed.
- API response retains stable bot contract with full attribution.
- KNW-03, KNW-04, and BOT-01 runtime grounding gaps are closed by automated tests.
</success_criteria>

<output>
After completion, create `.planning/phases/04-runtime-integration/04-02-SUMMARY.md`
</output>
