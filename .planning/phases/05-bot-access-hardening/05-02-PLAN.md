---
phase: 05-bot-access-hardening
plan: 02
type: execute
wave: 2
depends_on:
  - 05-01
files_modified:
  - src/core/config.py
  - src/bot/auth.py
  - src/bot/telegram_app.py
  - src/ai/langgraph/graph.py
  - src/ai/langgraph/nodes/decide.py
autonomous: true
requirements:
  - BOT-05
must_haves:
  truths:
    - "Доступ/роль вычисляются из whitelist `user_id` источника, а не hardcoded строки."
    - "Неавторизованный `user_id` стабильно получает deny-path до graph/retrieval/LLM."
    - "Авторизованный whitelist `user_id` проходит стандартный bot workflow."
  artifacts:
    - path: "src/core/config.py"
      provides: "Typed whitelist role source for Telegram access policy"
      contains: "telegram_user_roles"
    - path: "src/bot/auth.py"
      provides: "Unified authorization decision by Telegram user_id with reason codes and resolved role"
      contains: "authorize_telegram_user"
    - path: "src/bot/telegram_app.py"
      provides: "Transport-level deny-before-graph gate using shared user_id policy"
      contains: "handle_start"
    - path: "src/ai/langgraph/graph.py"
      provides: "Defense-in-depth auth node using shared user_id policy before retrieve"
      contains: "auth_node"
  key_links:
    - from: "src/bot/telegram_app.py"
      to: "src/bot/auth.py"
      via: "shared user_id authorization entrypoint"
      pattern: "authorize_telegram_user"
    - from: "src/ai/langgraph/graph.py"
      to: "src/bot/auth.py"
      via: "auth node re-check with same policy"
      pattern: "authorize_telegram_user"
    - from: "src/core/config.py"
      to: "src/bot/auth.py"
      via: "whitelist user role map"
      pattern: "telegram_user_roles"
---

<objective>
Implement BOT-05 production hardening by replacing hardcoded role resolution with shared whitelist `user_id` authorization across transport and graph.

Purpose: close milestone BOT-05 gap and enforce real access boundary for internal knowledge bot.
Output: config-backed user authorization source plus dual-layer deny protection (transport + graph auth node).
</objective>

<execution_context>
@.github/get-shit-done/workflows/execute-plan.md
@.github/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/v1-MILESTONE-AUDIT.md
@.planning/phases/05-bot-access-hardening/05-RESEARCH.md
@.planning/phases/05-bot-access-hardening/05-PATTERNS.md
@.planning/phases/05-bot-access-hardening/05-VALIDATION.md
@.planning/phases/03-telegram-bot/03-CONTEXT.md
@.planning/phases/03-telegram-bot/03-03-SUMMARY.md
@.planning/phases/04-runtime-integration/04-02-SUMMARY.md
@src/core/config.py
@src/bot/auth.py
@src/bot/telegram_app.py
@src/ai/langgraph/graph.py
@src/ai/langgraph/nodes/decide.py
@tests/phase3/test_auth_gate.py
@tests/phase5/test_access_policy.py

<interfaces>
From `src/core/config.py`:
```python
telegram_allowed_roles: set[str] = {"employee", "mentor", "admin"}
```

From `src/bot/auth.py`:
```python
class AuthDecision(BaseModel):
    allowed: bool
    reason: str
```

From `src/bot/telegram_app.py`:
```python
result = await graph.ainvoke(
    {
        "role": role,
        "query": update.message.text or "",
        "user_id": str(update.effective_user.id),
        "chat_id": str(update.effective_chat.id),
    },
    config={"configurable": {"thread_id": thread_id}},
)
```

From `src/ai/langgraph/graph.py`:
```python
def route_after_auth(state: GraphState) -> Literal["retrieve", "deny"]:
    return "retrieve" if state.get("authorized", False) else "deny"
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement whitelist user_id access policy contract in config and auth modules</name>
  <read_first>@src/core/config.py, @src/bot/auth.py, @tests/phase5/test_access_policy.py, @.planning/phases/05-bot-access-hardening/05-RESEARCH.md</read_first>
  <files>src/core/config.py, src/bot/auth.py</files>
  <behavior>
    - Test 1: unknown user_id resolves to deny with reason `not_whitelisted`.
    - Test 2: mapped user with non-allowed role resolves to deny with reason `role_not_allowed`.
    - Test 3: mapped user with allowed role resolves to allow and exposes resolved role for downstream workflow.
  </behavior>
  <action>Add typed whitelist role mapping in settings (e.g., `telegram_user_roles`) and implement a single shared `authorize_telegram_user(user_id)` decision API in `src/bot/auth.py` returning allow/deny reason plus resolved role. Keep role vocabulary validation against `telegram_allowed_roles` and preserve deny-answer envelope semantics. This directly implements BOT-05 success criterion #1 and replaces hardcoded role source.</action>
  <acceptance_criteria>Authorization decisions are driven only by whitelist user_id map + allowed role set; no hardcoded fallback role exists in production path.</acceptance_criteria>
  <verify>
    <automated>uv run pytest tests/phase5/test_access_policy.py -q</automated>
  </verify>
  <done>Whitelist-backed authorization source is implemented and proven by unit tests.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire transport and graph to shared user_id policy with deny-before-retrieve invariants</name>
  <read_first>@src/bot/telegram_app.py, @src/ai/langgraph/graph.py, @src/ai/langgraph/nodes/decide.py, @tests/phase3/test_auth_gate.py</read_first>
  <files>src/bot/telegram_app.py, src/ai/langgraph/graph.py, src/ai/langgraph/nodes/decide.py</files>
  <behavior>
    - Test 1: unauthorized user_id at `/start` and message handlers returns deny response and never awaits `graph.ainvoke`.
    - Test 2: authorized user_id invokes graph with resolved role and stable thread envelope.
    - Test 3: graph auth node denies unauthorized identity and retrieve path is unreachable.
  </behavior>
  <action>Replace transport role resolution with shared `authorize_telegram_user` lookup using Telegram `effective_user.id`; enforce immediate deny return on disallowed users in both `/start` and message handlers. In graph auth node, re-check authorization with the same shared policy using `state.user_id` for defense-in-depth before routing to retrieve. Keep `decide` behavior consistent with graph auth source (remove/align duplicate role-only assumptions) so unauthorized identities cannot pass downstream by role string alone. This delivers BOT-05 success criteria #2 and #3.</action>
  <acceptance_criteria>Unauthorized identity cannot trigger graph/retrieve in either transport or graph path; whitelisted users continue normal answer flow without contract breakage.</acceptance_criteria>
  <verify>
    <automated>uv run pytest tests/phase3/test_auth_gate.py tests/phase5/test_access_policy.py -q</automated>
  </verify>
  <done>Bot runtime enforces whitelist-based access at both trust boundaries and preserves authorized workflow.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Telegram client -> `src/bot/telegram_app.py` | Untrusted user identity enters authorization boundary |
| Transport layer -> LangGraph runtime | Authorization state controls access to retrieval/LLM/data |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-04 | E | `src/bot/telegram_app.py` | mitigate | Resolve access from whitelist `user_id` map and deny before `graph.ainvoke` for non-whitelisted users. |
| T-05-05 | I | `src/ai/langgraph/graph.py` | mitigate | Re-check authorization in `auth_node` and route unauthorized states directly to deny node before retrieve. |
| T-05-06 | T | `src/core/config.py` | mitigate | Validate mapped roles against `telegram_allowed_roles`; reject/deny invalid mappings with explicit reason code. |
</threat_model>

<verification>
- `uv run pytest tests/phase5/test_access_policy.py -q`
- `uv run pytest tests/phase3/test_auth_gate.py -q`
- `uv run pytest tests/phase5 tests/phase3/test_auth_gate.py -q`
</verification>

<success_criteria>
- BOT-05 is implemented via real whitelist `user_id` source (no hardcoded auth role).
- Unauthorized `user_id` is denied before graph/retrieve/LLM execution.
- Authorized whitelist `user_id` completes existing bot flow with no contract regression.
</success_criteria>

<output>
After completion, create `.planning/phases/05-bot-access-hardening/05-02-SUMMARY.md`
</output>
