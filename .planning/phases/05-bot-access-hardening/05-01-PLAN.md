---
phase: 05-bot-access-hardening
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/phase5/test_access_policy.py
  - tests/phase3/test_auth_gate.py
autonomous: true
requirements:
  - BOT-05
must_haves:
  truths:
    - "Неавторизованный Telegram user_id получает deny-path без вызова graph/retrieval."
    - "Авторизованный whitelist user_id проходит в обычный workflow бота."
    - "Решение доступа определяется по источнику whitelist user_id, а не через hardcoded роль."
  artifacts:
    - path: "tests/phase5/test_access_policy.py"
      provides: "Unit tests для whitelist allow/deny policy и reason codes"
      contains: "test_authorize_user_not_whitelisted_denied"
    - path: "tests/phase3/test_auth_gate.py"
      provides: "Transport/graph integration tests, доказывающие deny-before-graph и allow-path"
      contains: "test_start_unauthorized_returns_deny_and_skips_graph"
  key_links:
    - from: "tests/phase3/test_auth_gate.py"
      to: "src/bot/telegram_app.py"
      via: "handler invocation + graph.ainvoke assertions"
      pattern: "assert_not_awaited|assert_awaited_once"
    - from: "tests/phase5/test_access_policy.py"
      to: "src/bot/auth.py"
      via: "policy function reason/allowed assertions"
      pattern: "not_whitelisted|role_not_allowed|allowed"
---

<objective>
Create Wave 0 BOT-05 test scaffolding and convert auth tests to real whitelist `user_id` behavior before implementation.

Purpose: lock deterministic authorization behavior so Phase 5 implementation cannot regress into hardcoded role checks.
Output: failing/targeted tests that define BOT-05 contract end-to-end.
</objective>

<execution_context>
@.github/get-shit-done/workflows/execute-plan.md
@.github/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-bot-access-hardening/05-RESEARCH.md
@.planning/phases/05-bot-access-hardening/05-PATTERNS.md
@.planning/phases/05-bot-access-hardening/05-VALIDATION.md
@.planning/phases/03-telegram-bot/03-CONTEXT.md
@src/bot/auth.py
@src/bot/telegram_app.py
@src/ai/langgraph/graph.py
@tests/phase3/test_auth_gate.py

<interfaces>
From `src/bot/auth.py`:
```python
class AuthDecision(BaseModel):
    allowed: bool
    reason: str = Field(min_length=1)
```

From `src/bot/telegram_app.py`:
```python
async def handle_start(update: Update, context: ContextTypes.DEFAULT_TYPE) -> None
async def handle_message(update: Update, context: ContextTypes.DEFAULT_TYPE) -> None
```

From `src/ai/langgraph/graph.py`:
```python
def build_graph()
def route_after_auth(state: GraphState) -> Literal["retrieve", "deny"]
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add BOT-05 unit policy tests for whitelist-based authorization</name>
  <read_first>@.planning/phases/05-bot-access-hardening/05-VALIDATION.md, @src/bot/auth.py, @src/core/config.py</read_first>
  <files>tests/phase5/test_access_policy.py</files>
  <action>Create `tests/phase5/test_access_policy.py` with deterministic unit tests for whitelist policy outcomes: unknown `user_id` -> deny (`not_whitelisted`), mapped disallowed role -> deny (`role_not_allowed`), mapped allowed role -> allow (`allowed`). Encode BOT-05 contract explicitly so access is identity-bound; this enforces roadmap goal and prevents reintroducing hardcoded-role behavior.</action>
  <acceptance_criteria>Test file exists and contains explicit assertions for both `allowed` boolean and `reason` code in all three policy branches.</acceptance_criteria>
  <verify>
    <automated>uv run pytest tests/phase5/test_access_policy.py -q</automated>
  </verify>
  <done>Policy contract for whitelist-based access is executable and deterministic for BOT-05 evidence.</done>
</task>

<task type="auto">
  <name>Task 2: Refactor transport/graph auth tests to use whitelist user_id source</name>
  <read_first>@tests/phase3/test_auth_gate.py, @src/bot/telegram_app.py, @src/ai/langgraph/graph.py, @.planning/phases/03-telegram-bot/03-CONTEXT.md</read_first>
  <files>tests/phase3/test_auth_gate.py</files>
  <action>Rewrite auth-gate tests to prove user_id-based authorization per D-11: unauthorized `/start` and message paths deny and skip `graph.ainvoke`, authorized whitelisted `user_id` invokes graph with thread envelope, and graph deny branch short-circuits retrieve for unauthorized identity. Avoid role-only monkeypatch assertions as the primary signal; assertions must anchor on whitelist-backed authorization intent.</action>
  <acceptance_criteria>Tests assert deny-before-graph for unknown users and allow-path for whitelisted users using deterministic fixtures; retrieve short-circuit assertion remains present for defense-in-depth.</acceptance_criteria>
  <verify>
    <automated>uv run pytest tests/phase3/test_auth_gate.py -q</automated>
  </verify>
  <done>Phase 3 auth regression suite now encodes real BOT-05 hardening behavior and is ready to drive implementation.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Telegram update -> bot transport | Untrusted external user identity crosses into authorization gate |
| bot transport -> graph runtime | Authorized state controls whether retrieval/LLM path can run |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | E | `tests/phase3/test_auth_gate.py` | mitigate | Enforce deny-before-graph assertions for unauthorized `user_id` so privilege escalation regressions fail tests. |
| T-05-02 | I | `tests/phase3/test_auth_gate.py` | mitigate | Keep retrieve-short-circuit test that fails if unauthorized path reaches retrieval. |
| T-05-03 | T | `tests/phase5/test_access_policy.py` | mitigate | Assert exact reason codes for whitelist/role mismatch to detect policy tampering or accidental logic drift. |
</threat_model>

<verification>
- `uv run pytest tests/phase5/test_access_policy.py -q`
- `uv run pytest tests/phase3/test_auth_gate.py -q`
</verification>

<success_criteria>
- BOT-05 authorization behavior is specified in automated tests before implementation.
- Deny-before-graph and deny-before-retrieve invariants are covered by deterministic assertions.
- Tests express whitelist `user_id` source semantics rather than hardcoded role assumptions.
</success_criteria>

<output>
After completion, create `.planning/phases/05-bot-access-hardening/05-01-SUMMARY.md`
</output>
